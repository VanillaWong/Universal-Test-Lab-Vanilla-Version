# Universal Test Lab — 修改归档（2026-08-28）

> 本文件记录截至 2026-08-28 的全部改动、待办与已知限制，供对话压缩后继续工作。

## 12. 2026-08-30 第十三部分归档（UI 重构完成：EXPERIMENTAL 标签——Ground/Flight Configure 嵌入；最新编译 16:44:57）

- 新增 `FlightConfigurePanel`（复制 ModernFlightConfigureWindow：燃料滑条+数字框 / 机枪弹链组（GunBeltFamily） / 干扰弹站+1/8 分位游戏内预览）
- 新增 `GroundConfigurePanel`（复制 ModernGroundConfigureWindow ~700 行：跨域 cannon 注入 / CANNON 选择器（Ask3lad 折叠） / 弹药搜索列表 / 4 槽编辑器 / 弹道+机动调参 / 无限弹药 / CLEAR / RESET ALL）
- `BuildExperimentalTab()`：ShowWorkspaceTab(4) 首次触发懒构建 + REFRESH FOR CURRENT VEHICLE（切车后重建）+ APPLY CONFIGURATION（Collect → WorkspaceSetSettings + 全局 cannon 记忆 + 状态栏）
- GroundSelected → GroundConfigurePanel；飞机 → FlightConfigurePanel
- 新增 `--screenshot-experimental` 开关（tab index4）
- **UI 重构至此全部完成**：VEHICLE / TARGETS / OPTIONS / GARAGE / EXPERIMENTAL 5 个标签全部填充
- 验证：编译 16:44:57 exit=0 · selftest 全绿 · 5 个标签截图全部 exit=0
- 遗留：独立窗口（Map/Configure/Options）保留原样（与面板双份，注释已提醒同步）；era_presets 命名提示仍是旧 tsv 文案

## 11. 2026-08-30 第十二部分归档（UI 重构：TARGETS 标签——Map & Scenario 嵌入；最新编译 16:30:29）

- 新增 `MapPanel` 控件（复制自 ModernMapWindow：SCENARIO MODE / ERA PRESET+SAVE / combined spawn / 4 飞行目标 / 7 地面位置+敌我开关 / 舰船+被动开关），`Collect()` 返回 MapPanelResult（combined 未选地图/出生点时返回 null）
- 新增 `MapPanelState`（构造入参：当前全部目标配置）与 `MapPanelResult`
- 主界面 `BuildTargetsTab()`（TARGETS 标签嵌入 MapPanel + APPLY TARGETS 按钮）+ `ApplyTargetsPanel()`（写回 airTarget/airCount/airTarget01/heli*×2/configuredGroundTargets/hostile/ship/passiveShip/combinedScenario + UpdateConfigurationSummary）
- BuildTargetsTab 在 PopulateControls 末尾调用（依赖 groundTarget/shipTarget ItemsSource 与 aircraftViews，WireEvents 太早）
- 新增 `--screenshot-targets` 开关（tab index1）
- 踩坑：controller.WorkspaceCombinedMaps 是 IList（.ToList()）
- 验证：编译 16:30:29 exit=0 · selftest 全绿 · targets/ground/options/garage 截图全 exit=0
- 待办：EXPERIMENTAL（Ground/Flight Configure 搬入）——最后剩余标签

## 10. 2026-08-30 第十一部分归档（UI 重构：GARAGE 标签——Recently Used / Favourites / Ammo Presets；最新编译 16:07:36）

- GARAGE 标签三列：RECENTLY USED（选车自动记录，最多 20，点击跳转）/ FAVOURITES（★ 收藏当前 + 移除）/ AMMO PRESETS（SAVE CURRENT AS PRESET… / LOAD SELECTED / DELETE SELECTED，数据在 config.json ammo_loadouts）
- config["garage"] = {"recently_used": [...], "favourites": [...]}
- `TrackVehicleSelection()` 挂在 AircraftChanged 后；`suppressGarageTrack` 防止 SessionRestore/程序化选车误记录
- `SelectVehicleById`：跨类型跳转（自动切 typeFilter，找不到时清空过滤重试）
- ModernShellStorage 重构：抽出 SaveAmmoPresets + 新增 DeleteAmmoPreset
- 踩坑记录：
  - BuildGarageTab/RefreshGarageLists 必须在 aircraftViews 填充后（WireEvents 早于 PopulateControls → aircraftViews null → ArgumentNullException）——刷新调用移到 PopulateControls 末尾
  - `IEnumerable`（非泛型）需全限定名 System.Collections.IEnumerable
  - AircraftSettings.GroundAmmoLoadouts 是 readonly（用 Clear + Add）
- 新增 `--screenshot-garage` 开关
- 验证：编译 16:07:36 exit=0 · selftest 全绿 · ground/options/garage 截图全 exit=0
- 待办：TARGETS（Map & Scenario 搬入）/ EXPERIMENTAL（Ground/Flight Configure 搬入）

## 09. 2026-08-30 第十部分归档（UI 重构：OPTIONS 标签嵌入 Mission Options；最新编译 15:53:36）

- 新增 `MissionOptionsPanel : StackPanel`（从 ModernMissionOptionsWindow 复制内容构建：重生延迟/目标恢复/再装填/弹药策略/出生模式/速度滑块+Rapid Fire 全套，含 Card/OptionCard/FormatSeconds/Apply()）
- 主界面 `BuildOptionsTab()`：OPTIONS 标签内嵌 panel + APPLY OPTIONS 按钮（应用即写 MissionSettings.Current + 存 config）
- 窗口保持原样（双份内容构建，注释标注需同步；后续可让窗口复用 panel）
- 新增 `SelectWorkspaceTabForScreenshot` + `ModernUi.RenderOptions` + `--screenshot-options <path>` 开关（OPTIONS 标签截图验证）
- 验证：编译 15:53:36 exit=0 · selftest 全绿 · screenshot-options exit=0 · screenshot-ground exit=0
- 遗留：TARGETS（Map 搬入，最大）/ EXPERIMENTAL（Ground/Flight Configure）/ GARAGE（新功能）标签待填充；MissionOptionsPanel 与窗口内容双份待合并

## 08. 2026-08-30 第九部分归档（session 恢复修复：飞机不再每次回退 ef2000；最新编译 15:47:21）

### 用户报告
每次关掉重开 UTL 都回到 ef2000 aesa，飞机选择不保留。

### 根因（两层）
1. **SessionRestore 恢复失败**：`aircraftList.Items.Cast<AircraftView>()` 在 ItemsSource 模式下枚举不可靠（窗口未布局时为空）→ 选中赋值失败 → PopulateControls 兜底强制 ef2000 → **关闭时 SessionSave 又把 ef2000 存回 session → 死循环**（保存的永远是 ef2000，重开自然恢复 ef2000）
2. **ConfigStore 读操作不触发加载**：GetString/GetObject/GetList 在任何写操作之前调用会读到空 data（--selftest-session 里 expected 为空的直接原因）

### 修复
- SessionRestore 的 vehicle_id 恢复改为直接 `aircraftList.SelectedItem = saved`（ItemsSource 与 aircraftViews 共享对象引用，直接赋值 WPF 自动匹配）；兜底逻辑同样去掉 Items.Cast
- ConfigStore.GetString/GetObject/GetList 开头 `var d = Data;`（先触发加载，loaded 防重复）
- 新增 `SessionSelectedVehicleId` 属性 + `--selftest-session <dir>` 开关（构造窗口→消息泵→检查实际选中项）

### 验证
- real-config（session=ef_2000_aesa）→ actual=ef_2000_aesa PASS
- 模拟 fa_18e_block_2 → actual=fa_18e_block_2 PASS（修复前 FAIL）
- 模拟地面车 ussr_bmpt → actual=ussr_bmpt PASS
- selftest 全绿 · smoke exit=0 · 编译 15:47:21

### 用户实测
重新选 fa_18e（或其他飞机）→ 关闭 UTL → 重开应保留。注意：当前真实 config.json 的 session 已被覆盖成 ef_2000_aesa（死循环期间），选一次新飞机后即恢复正常。

## 07. 2026-08-30 第八部分归档（A2 阶段2 完成：统一 config.json；最新编译 15:38:05）

### 目标
%LOCALAPPDATA%\UniversalTestLab\config.json 统一管理 6 个散文件（game_folder.txt / mission_options.txt / aircraft_settings.txt / era_presets.tsv / ammo_loadouts.tsv / session.txt）。

### 结构（可读）
```json
{
 "game_folder": "C:\\...\\War Thunder",
 "mission_options": { "player_respawn_delay": 0, "target_respawn_delay": 5, "rapid_fire_interval": 0.5, ... },
 "aircraft_settings": { "f_15c_golden_eagle": { "override_countermeasures": true, "flare_rounds": 410, "countermeasure_loadouts": { "emtr_flare1": { "flares": 30, "chaff": 30 }, ... } } },
 "era_presets": [ { "name": "WWI - 1916", "ground": [...], "air": [...], "air_counts": [2,2,0,0], "ship": "...", "ship_count": 1 } ],
 "ammo_loadouts": [ { "name": "...", "vehicle_id": "...", "slots": [ { "slot": 0, "count": 44, "source_blk": "...", "bullet_name": "..." } ] } ],
 "session": { "vehicle_id": "...", "vehicle_kind": "...", ... }
}
```

### 实现
- **Json / ConfigStore 静态类**（UniversalTestLab.cs，SettingsStore 前）：懒加载、自动迁移、原子写（tmp+rename）
- **aircraft_settings**：base64+管道串 → 可读对象（只写非默认字段；golden_eagle 的 8 个发射器 flares/chaff 全部可读）——PresetStore.SerializeSettingsJson / DeserializeSettingsJson（保留原管道序列化器不动，兼容旧 preset）
- **迁移**：无 config.json → 读全部旧文件 → 写 config.json；旧文件保留不删；损坏自动重迁移
- **接入点改造**：SettingsStore（game_folder）/ MissionSettings（mission_options）/ LoadAircraftSettings+PersistAircraftSettings / ModernShellStorage（ammo_loadouts）/ LoadEraPresets / SavePresetClicked（era 保存，内置预设兜底）/ SessionSave+SessionRestore
- **测试开关**：--config-dir <dir>（重定向配置根，测试不污染用户目录）+ --selftest-config <dir>（迁移自检）

### 关键 bug（测试中抓到，重要）
SetString/SetObject/SetList/Save **必须先触发 Data 加载**——否则 GUI 启动早期 SessionSave 用空 data 覆盖完整 config.json（aircraft_settings 等全部丢失）。已修：所有写操作开头 `var d = Data;`。

### 验证（全绿）
- init 迁移：aircraft=65 era=5 mission=10 session=13 ✓
- smoke（GUI 路径）后 config 完整保留（aircraft=65）✓
- reload 从 config.json 加载 ✓；gc 0ms source=prebuilt ✓；selftest 全绿；编译 15:38:05
- 注意：GUI 启动后 session 被 GUI 当前状态覆盖（13→11 keys，预期行为）

### 下一步
- 用户实测：BMPT 新弹药 UI（ATGM 2 槽数量）+ 首次运行新 exe 真实目录迁移确认
- 待办剩余：部分修部件名实测（drive_wheel/idler/autoloader）、BUK 目录验证
- UI 重构剩余标签：TARGETS/OPTIONS/GARAGE/EXPERIMENTAL 填充

## 06. 2026-08-30 第七部分归档（弹药 UI 复刻原版试驾：按武器分组多槽；最新编译 15:01:55）

### 需求（用户确认）
- BMPT：两门 30mm 各自独立弹药槽（各 1 条弹链，可调 0）+ 130mm ATGM 2 个槽（2 种导弹各 1 槽，总量≤4，数量独立输入）
- 四号G（it_pzkpfw_iv_ausf_g）：75mm 4 个槽（5 种弹药选 4，总量≤87）· M16：弹链 24 条可调（0-24）

### 实现
- **数据层**：belt_type_limits.tsv（158 辆 belt_type_limit>1，数值=游戏事实，参考 Ask3lad 但自产）；json 新增 beltTypeLimit（per vehicle）
- **槽分配**（BuildGroundAmmoSlotGroups）：belt 武器（≤40mm）=min(beltTypeLimit,弹药种类)槽；炮弹武器=min(种类,剩余槽)；总槽≤4；无弹药选项武器（81mm tucha 烟雾弹发射器）不占槽；纯机枪车机枪算主武器；空组兑底 STOCK-only
- **UI**：弹药工作区改按武器分组（标题 + 总量 x/y chains|rds + 槽行 WrapPanel），ScrollViewer 限高 360
- **保存**：每槽 clamp 0-上限；同武器槽总和超上限从尾裁剪；count=0=空槽不写 mission；bulletsN 顺序=游戏 UI 顺序（30mm1→30mm2→ATGM槽1→槽2），BMPT ATGM count 问题应随槽位对齐解决
- belt_type_limit=2 已接入（BMP-2=2 / PGZ-09=2 / Wiesel=4 等）

### 验证（数据层 + 全绿）
- BMPT：30mm×2 各 1 槽 max1 链 + ATGM 2 槽 max4 ✓（与用户描述原版一致）
- 四号G：75mm 4 槽 max87 ✓ · T-72M1：125mm 4 槽 max44 ✓ · M16：12.7mm 1 槽 24 链 ✓ · BMP-2：30mm 2 槽 + 125mm 导弹 1 槽（tucha 不占槽）✓
- 编译 15:01:55 exit=0 · selftest 全绿 · ground-cache 0ms source=prebuilt · 冒烟 exit=0

### 待用户游戏内验证
- BMPT：ATGM 设 2 槽数量后进游戏实际发数（槽位对齐后应生效）
- BMP-2M/PGZ-09：30mm/35mm 2 槽是否按预期 · 四号G：75mm 4 槽 + 总量 87

## 05. 2026-08-30 第六部分归档（M16 MGMC 修复：多联装/口径解析/beltSize；最新编译 14:08:06）

### 用户报告
- 选 M16 MGMC 只显示 `7mm:0/1 chains`（应为 12mm 四联 M2，弹链 24 条）

### 根因（3 个 bug）
1. **GroundCalibre 正则 `(\d+)mm` 把 `12_7mm_M2...` 解析成 7mm**（正则在 "12_7mm" 里先匹配 "7mm"）→ 修复为 `(\d+)(?:\_\d+)?mm`（12_7mm→12，7_62mm→7；Build 侧 beltSizes 口径提取同一正则）
2. **多联装被去重**：M16 四联 M2 = 4 个相同 Weapon 块（同 trigger gunner0 + 同 blk + 各 1200 发），旧 seen 去重只留 1 个（1200）→ 新规则：**连续相同 (trigger,blk) 块 = 多联装累加（M16=4×1200=4800）；非连续重复 = 游戏数据冗余忽略（sw_t_72m1 的 Weapon 组重复出现，125mm 只算 44 不翻倍）**——Build-Catalog.ps1（$lastWkey/$seenWeapons）与 WorkspaceGroundWeaponsUncached（lastKey/seen）两侧一致
3. **beltSize 错用单枪 bullets:i=1200** → 正确来源是武器 blk 的 **bulletsCartridge:i**（弹链容量；M2=200）→ 4800/200=**24 条链** ✓；vehicle_weapons.json 新增 beltSizes 字段（per 口径，1083 辆车有），程序 calibreBeltSize 优先 BeltSizes（回退 w.NativeAmmo）

### 验证
- M16：weapons=1 total=4800 beltSizes{"12":200} chains=24 ✓（用户期望值）
- T-72M1：125mm=44（非连续重复正确忽略）；weapons=125/7.62/12.7/dummy/tucha
- BMP-2：beltSizes{"7":250,"30":250} 与 Ask3lad {"30mm":250} 一致
- selftest 新增 M16 断言（NativeAmmo>=4800 + BeltSizes["12"]==200）；编译 14:08:06 exit=0（3 个既有 CS0649）；selftest 全绿；ground-cache first=4ms source=prebuilt；screenshot-ground exit=0

## 04. 2026-08-30 第五部分归档（武器配置对齐 Ask3lad；最新编译 13:49:39）

### 背景
用户要求参考 Ask3lad 的武器配置呈现（它不会把机枪和烟雾弹都搬出来）。

### Ask3lad 规则（从 2.52 DB 确认）
- Tank2.0_DB.json 无武器列表，只有 ammo[]（弹药名）+ ammo_limits{}（口径限额）+ 可选 belt_size/belt_type_limit（198 辆有，默认 1 最大 5，如 cn_wz_305=5；归档待办 3 的数据参考）
- 主炮弹药（含主炮 Smoke 弹如 105mm_M416_Smoke）→ 显示（335 辆）
- 机炮/导弹/火箭炮 20mm+ → 显示
- 次要机枪（有主炮时）→ 不显示
- 纯机枪车（M16 MGMC/CM52/BTR-152A 等 16 辆）→ 机枪弹药显示（它就是主武器）
- 烟雾弹发射器（34 种 blk，全是 *_smoke_*_launcher）→ 从不显示
- config.json 结构：WT_DIR/db_version/app_version/ground_recently_used/ground_favourites/naval_*/user_*_presets/user_ammo_loadouts/dark_mode

### 改动（ModernShell.cs）
1. 主界面弹药工作区：删除弹药总览列表 groundAmmoList + MOUNT SELECTED ROUND 按钮 + GroundMountSelected/GroundExclusionKey（4 槽下拉已承担选择，列表冗余；槽下拉 ToString()=Text 与列表同文本；另发现双击事件重复绑定两次的小 bug 一并消失）
2. 布局紧凑：slots Row4→3、presetRow Row6→4
3. Ask3lad 对齐过滤：新增 groundHasMainWeapon + IsSecondaryGroundWeapon（blk 含 machinegun/smoke，internal static 供两个类用）；有主武器时隐藏 <20mm 机枪口径（STOCK 池/弹链/池文本三处），纯机枪车保留
4. Ground Configure CANNON 下拉折叠：主武器条目 + disabled 分隔符 "— SECONDARY (MACHINE GUNS / SMOKE) —" + 灰色次要武器条目；纯机枪车次要武器正常显示（CannonSelectorChanged 对 null Tag 安全）

### 验证
- 数据层：过滤 12 个机枪弹链（有主炮车），保留 46 个（13 辆纯机枪车）；主炮 Smoke 弹 343 个保留（Ask3lad 335）；**M16 MGMC 弹药与 Ask3lad 完全一致**（12mm_usa_M2HB 四种）；T-72M1 无 12mm
- 编译 13:49:39 exit=0（3 个既有 CS0649）；selftest 全绿；ground-cache first=0ms source=prebuilt；screenshot-ground exit=0

## 03. 2026-08-30 第四部分归档（A2 阶段1 完成：vehicle_weapons.json 预构建；最新编译 12:38:38）

### 本轮完成：A2 阶段1（选车/查弹药 = 查字典零解析，Ask3lad 模式）

- **Build-Catalog.ps1**：
 - 新增 `Get-WeaponFile` / `Escape-Json` / `ConvertTo-JsonManual`（手写 StringBuilder JSON 构建器——PS5.1 下 JavaScriptSerializer 对 hashtable 有 PSParameterizedProperty 循环引用死结，BaseObject 解包无效；ConvertTo-Json 有单元素数组变对象的 bug；手写已 round-trip 实测）
 - 新增 `Build-VehicleWeaponsJson`（Phase5/5）：遍历 tankmodels 1338 个 blk，提取 weapons（trigger/blk/nativeAmmo，与 WorkspaceGroundWeapons 同逻辑）/ missiles（WeaponPilons→Slot→Preset→Weapon，与 WorkspaceVehicleMissiles 同逻辑）/ beltOptions（modifications 的 `\d+mm_` 非 _ammo_pack 空模块）/ rackRounds（launcher/container 武器的 bullets:i>1）；1284 辆车有数据，输出 `data/vehicle_weapons.json`（603KB）
 - 新增 `-PhaseOnly 5` 参数：跳过 Phase1-4 只生成 json（51 秒）。实现：Phase1-2 直接代码 + Phase3-4 调用用 `if ($PhaseOnly -ne '5')` 包裹（函数定义区保留）；**注意早退块不能放 L332**（函数定义在脚本后部，PowerShell 顺序执行未注册 → CommandNotFound，已改为 if 包裹方案）
 - 进度粒度 200→100（aircraft/slots 两处）
 - **PhaseOnly5 必须显式传全部根参数**（L92 顶层加载语言文件需要 LangRoot 等，默认值指向项目上一级不存在）
- **Run-Task-Progress.ps1**：`$lastPos`/`$finished` 值类型闭包 bug——事件块赋值不回写外层，导致每次 Tick 从文件头重读、log 不断重复追加；修复为 `$script:` 前缀
- **UniversalTestLab.cs**：
 - DTO：`GroundWeaponCacheJson`（weapons/missiles/beltOptions/rackRounds，属性名与 json 键完全一致）
 - `LoadPrebuiltGroundWeapons()` 静态方法：JavaScriptSerializer（MaxJsonLength=int.MaxValue）反序列化 Embedded 资源 → `Dictionary<string, GroundWeaponCacheData>`（转 KeyValuePair）
 - `prebuiltGroundWeapons` 静态字典，LoadCatalogs 启动时加载
 - `WorkspaceGetGroundWeaponCache`：prebuilt 命中直接用（含 rackRounds 预填），缺失回退实时解析（GroundWeaponCache 保留）
 - selftest 新增断言：prebuilt >1000 辆 + sw_t_72m1 抽查
 - `--selftest-ground-cache` 输出 source=prebuilt/live
- **build.rsp**：+`/reference:System.Web.Extensions.dll` +`/resource:data\vehicle_weapons.json,UTL.vehicle_weapons.json`

### 验证（统一流程全过）

- 编译 12:38:38 exit=0（仅 3 个既有 CS0649 警告）；exe 41,064,448 B
- `--selftest-ground-cache`：**GROUND-CACHE first=0ms second=0ms cache-hit=yes source=prebuilt weapons=5 missiles=0 belt-options=4**（T-72M1 与实时解析 5/0/4 完全一致；比上轮 23ms 再降为 0ms）
- selftest 全绿；--screenshot-ground exit=0

### 下一步

1. A2 阶段2：用户配置合并统一 config.json（aircraft_settings.txt / mission_options.txt / era_presets.tsv / ammo_loadouts.tsv）——先备份再动
2. UI 重构剩余标签填充（TARGETS/OPTIONS/GARAGE/EXPERIMENTAL）
3. belt_type_limit=2（BMP-2M/PGZ）
4. 部分修部件名实测（drive_wheel/idler/autoloader）
5. 验证 BUK 在目录（重跑 Build-Catalog 全流程或检查 tsv）

## 02. 2026-08-30 第三部分归档（GroundWeaponCache 落地；最新编译 12:24:52）

### 本轮完成：GroundWeaponCache 内存缓存（解决选车卡顿）

- **UniversalTestLab.cs**：
 - 新增 `internal sealed class GroundWeaponCacheData`（Weapons / Missiles / BeltOptions / RackRounds 字典），定义在 GroundWeaponInfo 后
 - MainForm 新增 `groundWeaponCacheMap`（Dictionary<vehicleId, GroundWeaponCacheData>，OrdinalIgnoreCase）
 - `WorkspaceGetGroundWeaponCache(Aircraft)`：首次构建（内部调用三个 Uncached 解析），之后字典命中直接返回；null/空 Id 返回 null
 - `WorkspaceRackRoundsCached(cache, blk)`：按 blk 路径缓存架数，cache 为 null 时回退原逻辑
 - `WorkspaceGroundWeapons / WorkspaceVehicleMissiles / WorkspaceGunBeltOptions` 公共方法改为走缓存（原实现改名 `*Uncached` 私有方法，防递归）
 - **失效**：`WorkspaceGameFolder` setter 清空 groundWeaponCacheMap（游戏目录变更时缓存作废）
- **ModernShell.cs GroundRefreshAmmo**：全部 4 个数据源改从 `WorkspaceGetGroundWeaponCache(selectedAircraft)` 取（weapons / RackRounds / missiles 两处 / beltOptions），只解析一次；附带 ModernGroundConfigureWindow 的调用也自动走缓存
- **新增自检开关 `--selftest-ground-cache`**：构造 MainForm → 用 sw_t_72m1 调 WorkspaceGetGroundWeaponCache 两次计时 → 输出 first/second ms + cache-hit + 结果计数。无有效游戏目录时输出 SKIP
- 验证：GROUND-CACHE first=23ms second=0ms cache-hit=yes weapons=5 missiles=0 belt-options=4；selftest 全绿；冒烟 exit=0

## 00. 2026-08-30 追加记录（UI 重构计划已确认；最新编译 09:45:50）

### 本轮完成

- **Rapid Fire（定时自动维修+补弹）**：Mission Options 新增 RAPID FIRE 卡（ENABLED 开关默认关 = 什么都不修 + REARM INTERVAL 0.1-10s 默认 0.5）；mission 生成 `"UTL Rapid Fire"` 触发器块（`periodicEvent` + `unitRestore`，纯定时无需按键——飞机 F 键保持襟翼）；手动 F 键恢复（"UTL Quick Restore"）保留可共存
- **Rapid Fire 双模式（互斥，默认 ENABLED 关）**：
  - **FULL RESTORE**（默认勾选）：`fullRestore:b=yes`（全部修复 + 灭火 + 乘员复活）
  - **PARTIAL RESTORE**：`partRestore:b=yes` + **12 个 tankPart**（gun_barrel / gun_breech / engine / track_l / track_r / transmission / steering_gear / drive_turret_h(炮塔旋转,确认) / drive_turret_v(火炮俯仰,确认) / drive_wheel(主动轮,推测) / idler(惰轮,推测) / autoloader(自动装弹机,推测)）+ ammoRestore + fuelRestore；fullRestore:b=no
  - 持久化：`rapid_fire_enabled` / `rapid_fire_interval` / `rapid_fire_full_restore`（默认 1=全修）
  - 生成时 `ConfigureRapidFire` 按设置替换 unitRestore 块（FullRestoreAction / PartialRestoreAction 双形态常量，模板不用改）
- **ConfigureRapidFire `$10.5` bug（导致游戏"can't open file"）**：正则替换 `"$1"+数字` 拼接产生 `"$10.5"`（$10 被当组 10）→ 每次生成都损坏 mission 的 time:r 行 → 修复为 `"${1}"`；全代码检查无其他同类
- **unitRestore 机制限制（CDK 186 个动作全扫确认）**：无灭火/乘员恢复参数；灭火+乘员复活**只有 fullRestore 隐藏包含**；**实测 fullRestore+tankPart 组合被游戏忽略（全修）**；partRestore 不灭火、不复活死亡乘员 → **部分修模式不含灭火/乘员**（只有全修模式有）；`power` 参数不是灭火/乘员；`tankPart` 上限 12 个
- **部分修部件名**：drive_turret_h/v_dm 从坦克 blk 的 dm 引用确认；主动轮/惰轮/装弹机为推测名（damage model 是二进制，文本数据无法确认；游戏不认的 tankPart 会被忽略，可能归入履带 track 部件）——**待用户实测三个推测名是否生效**
- **Ask3lad UI 分析**（main.py，PyQt6）：标签页分层（模式→功能子页→更深子页）、QGroupBox 标题分组、列表+按钮（Garage 标签=Recently Used/Favourites/Themed Presets/User Presets+Load/Rename/Delete/Import/Export/Save）、控件组合（复选框开关+灰色说明+滑块两端刻度+SpinBox）、目标分组框（图片+名称+Change+QDial 旋转拨盘）、车辆预览图 120×120 + In Mission vs Your Selection 对比、单一 Apply 按钮、Random 功能（Random Vehicle/Targets/Weather）

### UI 重构计划（用户已确认，待实施）

主界面三列 → Ask3lad 式标签页：**VEHICLE / TARGETS / OPTIONS / GARAGE / EXPERIMENTAL** + 底部统一 GENERATE

- **阶段1：标签页框架**（最大改动，分批）：VEHICLE=现有三列整体移入（选车列表+弹药工作区+车辆信息，加 Country/In Mission/Your Selection/Ammo Loadout 风格分组标题）；TARGETS=Map 窗口目标配置搬入（7 地面+4 飞行+1 海上，每目标一个分组框，ERA 预设按钮行保留）；OPTIONS=Mission Options 内容（Rapid Fire/重生/出生速度/弹药政策等）；GARAGE=收藏/预设中心；EXPERIMENTAL=Ground/Flight Configure 实验功能（换炮/弹道/REAL VEHICLE VALUES/对抗措施/武器注入）
- **阶段2：Garage**（最近使用 session 已有 + ★收藏新增持久化 + 主题预设按钮行 era_presets.tsv + 用户预设管理）
- **阶段3：控件完善**（启用复选框+说明文字+滑块两端刻度）
- **阶段4：预览图**（游戏贴图提取 120×120，最重，可砍）
- **随机车辆不做**（用户明确）
- 待确认：独立窗口（Map/Mission Options/Ground/Flight Configure）并入标签页后退役 or 保留入口；TARGETS 旋转拨盘做不做；预览图优先级

## 01. 2026-08-30 第二部分归档（上下文上限；最新编译 12:08:15）

### 本轮完成（编译 12:08:15，selftest 全绿）

- **标签页框架（阶段1第一步）**：主界面顶部 5 个标签 `[VEHICLE][TARGETS][OPTIONS][GARAGE][EXPERIMENTAL]`（Toggle 互斥）——VEHICLE=现有三列原样移入（`x:Name=TabVehicleContent`，主体 Grid Row0 头部64/Row1 标签栏Auto/Row2 工作区*/Row3 状态栏28）；TARGETS/OPTIONS/GARAGE/EXPERIMENTAL=占位面板（`IsHitTestVisible=False`）；`ShowWorkspaceTab(0)` 初始化；**状态栏 Row3 修复**（之前三列 Grid 改 Row2 后状态栏被拉伸盖住整个工作区=“变暗+VEHICLE READY 走到中间”）
- **ExtractGameBlk 缓存（选车提速）**：原来每次调用新建 Guid 随机目录+重启 wt_ext_cli（选车 4-10 次进程）→ 改为按 blk 路径 FNV-1a 哈希固定目录（`cache\r{hash8}`，跨会话命中）+ `GetStableHash`
- **弹药 Ask3lad 模式**：`_ammo_pack` 词条过滤（WorkspaceGunBeltOptions）；主页弹药列表全部用**弹药改装件名**（无 _ammo_pack：BMP-2M=30mm_2a42_AP/APDS/APDS_FS/HE；T-72=125mm_ussr_3BM12_APDS_FS 等），不再显示 catalog bulletName（30mm_3ubr_11 无效）；数量：机炮=链数（belt_size 计算）、坦克炮=口径池发数（`groundNativeByCalibre`）
- **STOCK（默认弹药）+ 口径池**（Ask3lad 式）：弹药列表每口径一个 `NNmm STOCK (default ammo)` 条目（弹名空=游戏默认弹，数量照算）；列表上方口径池显示 `125mm: 44/44 rds`、`30mm: 2/2 chains`（`UpdateAmmoPoolText`）；STOCK 保存带口径标识（SourceBlk=`stock:125`），加载按口径找回；互斥按口径（`GroundExclusionKey`）
- **闪退修复（4 处）**：①序列化 `B64(x.SourceBlk)` 对 null 抛（Encoding.UTF8.GetBytes(null)）→ `?? String.Empty`（挂载弹药保存闪退）；②selectedAmmo 循环 `loadout.SourceBlk.Replace` 对 null 抛 → Where 加 `!String.IsNullOrWhiteSpace(x.SourceBlk)`（生成闪退）；③GroundFindEntry 对空弹名返回 null（stock 加载被清）→ 按 `stock:` 前缀+口径匹配；④GroundUpdateSettings stock 保存 SourceBlk=`stock:`+cal
- **生成侧对 stock（BulletName 空）安全**：ResolveGroundMissionAmmo 过滤改 `x.Count > 0`（允许 stock）；ammunitionSources 循环已有 `String.IsNullOrWhiteSpace(SourceBlk) continue` 防护；ConfigureGroundPlayer 空弹名写 `bullets:t=""`+count

### 选车卡顿（未完成，编辑被拒）

即使文件缓存命中，**每次选车仍重新解析 blk**（GroundRefreshAmmo：WorkspaceGroundWeapons 1 次 + VehicleMissiles 2 次 + GunBeltOptions 1 次 + RackRounds 每导弹一次）。Ask3lad 不卡=数据预构建内存 json。
**已设计未应用**（ModernShell.cs）：`GroundWeaponCache` 类（Weapons/Missiles/AmmoMods/RackRounds）+ `groundWeaponCache` 字典（vehicleId→缓存）+ `GetGroundWeaponCache()`（首次解析+缓存）+ GroundRefreshAmmo 改从缓存取（weapons/RackRounds/missiles/beltOptions 4 处）——**编辑被用户拒，需新会话重新应用**（或按 A2 方案整体 json 化替代）

### 用户决定：A2 —— 数据 json 化（彻底对齐 Ask3lad）

**A. 静态数据（catalog）**：载具/武器弹药/目标预设 → json 文件（启动一次加载到内存字典，选车/选弹药=查字典零解析）
- 现在：Build-Catalog 输出 tsv（ground.tsv/aircraft.tsv/ships.tsv/ground_ammo.tsv/modifications.tsv 等）→ 改为输出 json（嵌套结构）或 tsv→json 转换；加载逻辑改为内存字典
- Ask3lad DB 结构参考（GPL-3.0，**只能参考结构自产，不集成 json**）：Tank=ID/name/country/role/weapons_default/ammo_amount/ammo/ammo_loadouts/ammo_limits/belt_size/belt_type_limit；Plane=同结构但 ammo=["N/A"]；db_version.json=v3.51(2026-06-08)，坦克 1213/飞机 1322/直升机 112/舰船 577
**B. 用户配置**：载具设置/弹药挂载/地图目标/实验功能 → 统一 `config.json`（现在分散：aircraft_settings.txt/mission_options.txt/era_presets.tsv/ammo_loadouts.tsv）

### Ask3lad DB 构成（`C:\Users\honam\Desktop\Ask3lad Test Drive GUI 2.52\Ask3lad Test Drive GUI 2.52 Live Build\Ask3lad Test Drive GUI 2.52\Assets`）

Tank2.0_DB.json(457KB)/Plane2.0_DB.json(568KB)/Helicopter2.0_DB.json(50KB)/Ships2.0_DB.json(367KB)/Weapons2.0_DB.json(470KB)/AircraftWeapons2.0_DB.json(995KB)/NavalWeapons2.0_DB.json(351KB)/AmmoNames2.0_DB.json(49KB)/Vehicle_Previews/(预览图)/db_version.json

### 飞机差异（确认）

Ask3lad 飞机**只有 DB 数据没有生成**（Plane ammo=["N/A"]+Air 标签禁用）→ 长期做不出飞机试驾；**UTL 飞机完整生成**（flightmodel 处理/挂载注入/对抗措施/GunBelt 弹链）——比 Ask3lad 强

### 待办清单（新会话继续）

1. **A2 json 化**（用户已选）：Build-Catalog 输出 json + 启动加载内存字典 + 用户配置合并 config.json；先解决选车卡顿（可先应用 GroundWeaponCache 缓存过渡）
2. UI 重构剩余：TARGETS/OPTIONS/GARAGE/EXPERIMENTAL 标签填充（Map/Mission Options/Ground/Flight Configure 内容搬入）；阶段2 Garage 收藏/预设；阶段3 控件完善；阶段4 预览图（可砍）
3. belt_type_limit=2（BMP-2M/PGZ changeBulletTypeForSet）未实现；Pbv301/Bosvark=3 未采用
4. 部分修部件名（drive_wheel_dm/idler_dm/autoloader_dm 推测名）待用户实测是否生效
5. 部分修模式灭火/乘员：unitRestore 无参数，仅 fullRestore 含（确认限制）
6. 独立窗口（Map/Options/Ground/Flight Configure）并入标签页后退役 or 保留入口——待用户确认
7. 主界面 XAML 编辑注意：**文件里 XAML 是 verbatim 字符串（\"\" 转义）+ 2 空格缩进**（edit 时缩进必须精确）

## 02. 2026-08-30 第三部分归档（GroundWeaponCache 落地；最新编译 12:24:52）

### 本轮完成：GroundWeaponCache 内存缓存（解决选车卡顿，##01 中"编辑被拒"的方案现已应用）

- **UniversalTestLab.cs**：
  - 新增 `internal sealed class GroundWeaponCacheData`（Weapons / Missiles / BeltOptions / RackRounds 字典），定义在 GroundWeaponInfo 类后
  - MainForm 新增 `groundWeaponCacheMap`（Dictionary<vehicleId, GroundWeaponCacheData>，OrdinalIgnoreCase）
  - `WorkspaceGetGroundWeaponCache(Aircraft)`：首次构建（内部调三个 Uncached 解析），之后字典命中直接返回；null/空 Id 返回 null
  - `WorkspaceRackRoundsCached(cache, blk)`：按 blk 路径缓存架数；cache 为 null 回退原逻辑
  - `WorkspaceGroundWeapons / WorkspaceVehicleMissiles / WorkspaceGunBeltOptions` 公共方法改为走缓存（原实现改名 `*Uncached` 私有方法，防递归）
  - **失效**：`WorkspaceGameFolder` setter 清空 groundWeaponCacheMap（游戏目录变更时缓存作废）
- **ModernShell.cs GroundRefreshAmmo**：4 个数据源全部改从 `WorkspaceGetGroundWeaponCache(selectedAircraft)` 取（weapons / RackRounds / missiles 两处 / beltOptions），一次解析全复用
- 附带收益：ModernGroundConfigureWindow 的 `WorkspaceGroundWeapons(selectedAircraft)` 调用（L2031）自动走缓存

### 新增自检开关 `--selftest-ground-cache`

构造 MainForm → 用 sw_t_72m1 调 WorkspaceGetGroundWeaponCache 两次计时 → 输出 first/second ms + cache-hit + 结果计数；无有效游戏目录时输出 SKIP。用于缓存回归验证。

### 验证结果（统一流程全过）

- 编译：12:24:52 exit=0（仅 3 个既有 CS0649 警告，与本次无关）
- `--selftest-ground-cache`：**GROUND-CACHE first=23ms second=0ms cache-hit=yes weapons=5 missiles=0 belt-options=4**（T-72M1：5 武器槽 / 0 导弹 / 4 弹药改装件，合理）
- `--selftest`：全绿 exit=0
- `--screenshot-ground`：exit=0

### 下一步

1. **A2 json 化**（Build-Catalog 输出 json + 启动加载内存字典 + 用户配置合并 config.json）——选车卡顿已由本缓存缓解，继续 A2
2. UI 重构剩余标签填充（TARGETS/OPTIONS/GARAGE/EXPERIMENTAL）
3. belt_type_limit=2（BMP-2M/PGZ）
4. 部分修部件名实测（drive_wheel/idler/autoloader）
5. 验证 BUK 在目录（重跑 Build-Catalog）

## 0. 2026-08-29 追加记录（重要！最新编译 22:51）

- **代理单位分离（22:51 编译版）**：`GroundProxyClassId` = `userVehicles/ussr_t_26_1940`、`GroundProxyVehicleFileName` = `ussr_t_26_1940.blk`（UniversalTestLab.cs L2560/2561）——**不再用 us_m2a4**，与 Ask3lad 彻底互不干扰（us_m2a4 引用已清零）；改装件维持 mission 端 modification:t 列表（代理只含 include + 传感器/夜视/测距 @override + @delete commonWeapons 重定义）；旧任务（引用 userVehicles/us_m2a4）已作废需重新生成
- **ERA 时代预设（Map & Scenario）**：`ERA PRESET` 下拉（WWI 1916 / WWII 1943 / Gulf War 1991 / Modern 2020s），一键填充 7 地面 + 4 飞行 + 1 海上目标；预设数据**外部配置** `%LOCALAPPDATA%\UniversalTestLab\era_presets.tsv`（首次运行自动从内置释放，改它重启即生效，不用编译）；Map 窗口新增 **SAVE CURRENT AS PRESET**（命名→追加到配置文件→下拉即时刷新）
- **Map 4 个飞行目标可改**：AIR TARGET01（台风）/AIR TARGET02/HELI01（米-28NM）/HELI02（卡-52）各自可选机型+数量（0=不生成）；默认=模板原单位；选择随会话记忆
- **进度条工具** `Run-Task-Progress.ps1`：Build-Catalog/Update-Data 用 WPF 进度条窗口（按 Phase 分段 + 计数 + 日志），`-Task catalog|update`
- **快速重建工具** `Quick-GroundAmmo.ps1`：只重建 data/ground_ammo.tsv（扫 groundmodels_weapons 所有 blk，含导弹发射器）
- **导弹弹药目录**：Build-Catalog 的 Build-GroundAmmoCatalog 过滤器 `*user_*.blk` → `*.blk`（收录 SAM/ATGM 发射器弹：57E6/57E6M/TKB-1055/Milan/9M113 等）；SAM 类型显示优化
- **山毛榉（BUK）缺失修复**：units.csv 的 `Load-EnglishNames` 正则不支持嵌套引号（`""` 转义）→ BUK-M3 名称解析失败被过滤；已改为支持 `""` 转义（顺带修复其他带引号名称载具）
- **主页按车列弹药**：GROUND VEHICLE LAB 中间列改为 Grid 弹性布局（弹药列表 Star 填满容器、槽位/按钮固定底部）；中间列行高 86/72/48 → Auto（地面模式行塌陷，容器多出 ~206px）；**不按炮选择，直接列该车全部弹药**
- **主页弹药列表 = 炮弹 + 导弹挂载名**：`WorkspaceVehicleMissiles`（单位 blk 挂载方案 WeaponPreset 名）；机炮（≤40mm）= **弹链方案**（modifications 空模块 `30mm_2a38_HE`/`30mm_2a42_AP` 等，Ask3lad 同款）+ `WorkspaceGunBeltOptions`；坦克炮（>40mm）= catalog 炮弹（bulletName）；数量单位：机炮=**链数**（SM-SV 30mm = 1404÷351 = **4 chains**）、坦克炮/导弹=**发数**
- **任务槽弹药标识规则**（Ask3lad 验证）：炮弹→bulletName（30mm_UBR6）、导弹→**挂载方案名**（170mm_57e6_aam，bulletName 会崩）、飞机机炮→belt 类型名；导弹挂载名映射按**弹名相似度 + 每挂载只用一次**（57E6/57E6M 共用发射器不再全变 57e6_aam；PresetCore）；映射失败丢弃该槽防崩
- **导弹架数规则**（铠甲SM-SV）：12 架；57E6/57E6M=1 发/架（上限 12）、TKB-1055=4 发/架（上限 48）；总架数=Σ57E6 + Σ57E6M + ceil(TKB÷4) ≤12，超限从最后槽裁剪（TKB 被裁保持 4 的倍数）；发/架从发射器/容器文件 bullets:i 读（-1/1→1）
- **默认弹药 = Ask3lad 式**：不选弹药 → mission 槽 `bullets0-3:t=""` + `bulletsCount0=9999`（游戏用挂载方案原生默认，铠甲有预设机炮）；删掉了之前"主炮第一个炮弹"的默认填充
- **belt_type_limit**：游戏数据无该字段（Ask3lad 自有规则）；UTL 默认 1（同武器挂载互斥：同发射器换弹替换旧槽）；`changeBulletTypeForSet:b=true`（BMP-2M/PGZ）→ 2 未实现；Pbv301/Bosvark=3 是 Ask3lad 例外未采用
- **主页清理幽灵条目**：加载设置时 UI 找不到的旧条目（如被 belt 方案替代的 catalog 炮弹 30mm_UOF8）自动从 settings 移除并持久化；新增 **CLEAR ALL** 按钮（清空 4 槽 → 生成=游戏默认）
- **弹药预设 SAVE/LOAD**：`%LOCALAPPDATA%\UniversalTestLab\ammo_loadouts.tsv`（按车命名方案，含弹种+数量+Kind）
- **滑块数字输入框**：全部滑块（重生/补给/出生速度/燃油/对抗措施/弹药量）旁加 TextBox 双向绑定（`ModernNumericBox.Bind`）
- **原生弹药量显示**：导弹=架数×发/架（TKB=48）、机炮=链数（4）、坦克炮=发数；挂载默认=原生量，输入超限自动 clamp
- **雷达/激光测距修复**：effects 标量 → `"@override:字段名":类型=值` 语法（裸标量 user vehicle 加载器忽略）；全选时 modification 列表收集全部模块（含 laser_rangefinder_lws）
- **飞机设置持久化**：aircraft_settings.txt（上一轮 00:07 已有，本轮确认）
- **Ask3lad 源码分析**（`C:\Users\honam\Desktop\wt-testdrive-gui-2.0-2.52`，main.py）：任务槽原样写 DB 挂载名零校验；DB 从 GitHub（ask3lad/wt-testdrive-db）下载，extract_ammo.py 未公开；LICENSE=GPL-3.0（UTL 是 MIT → 不集成 json，参考结构自产=路线 B）；`_GROUND_PRESETS` 时代预设硬编码在 main.py；`_AMMO_POOL_ALIASES`/`_belt_sizes` 弹药池规则

## 1. 项目与编译状态

- 项目路径：`C:\Users\honam\Desktop\Universal-Test-Lab-0.12.0-beta.2_modified\Universal-Test-Lab-0.12.0-beta.2`
- exe：`dist\UniversalTestLab.exe`（最新编译 2026-08-28 19:47，约 38.4MB）
- **编译方式：直接跑 csc**（`Build.ps1` 在图标生成后 csc 不输出，不可靠）：
  ```
  Set-Location <项目根>
  & 'C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe' '/lib:C:\Windows\Microsoft.NET\Framework64\v4.0.30319\WPF' '@build.rsp'
  ```
- 模板（`template\universal_test_lab.blk`）与 `data\` 目录都是内嵌资源，**改模板/数据后必须重编译**
- 数据更新脚本：`Update-Data.ps1`（解包→重建目录→编译），用 `powershell -NoProfile -ExecutionPolicy Bypass -File .\Update-Data.ps1` 跑

## 2. 已完成改动清单（简要）

1. **坦克双发修复**：`@override:commonWeapons` → `@delete:commonWeapons{}`+重定义（对齐 custom_tu_95m 写法）
2. **老式飞机挂架**：Build-Catalog 预设型提取（517 架恢复）；任务 `weapons:t` 直接引用原生预设名
3. **"value cannot be null"**：PresetPath 空值保护
4. **数据重建进度** + Build-Catalog.ps1 加 UTF-8 BOM（PS5.1 可跑）
5. **机炮吊舱重量**：TotalMass 不再乘弹药数（cannon/machinegun 识别）
6. **Inject All 卡顿**：>400 条不分组 + Recycling 虚拟化 + 搜索/列宽防抖
7. **老飞机注入警告**（预设型挂架检测）
8. **Mission Options（全局）**：重生延迟（wait 方案）/靶标恢复/补弹时间/弹药模式；MissionSettings 持久化（`%LOCALAPPDATA%\UniversalTestLab\mission_options.txt`）；0.1s 滑块
9. **出生速度**：maxSpeed clamp + 机场起飞（静止）
10. **跨域炮注入**（CROSS-DOMAIN CANNON）：GROUND/NAVAL/AIRCRAFT/HELICOPTER 四域 → 单位 → 武器三级选择；注入配置随载具/全局记忆
11. **FIRE RATE OVERRIDE**：独立生效；0=原生射速（写 Weapon 块 shotFreq，修复 cannon 级无效）
12. **注入弹药 9999**：Weapon 块 `bullets:i=9999`（正则写入）；注入炮 HUD 弹种（bullets0 组名）
13. **多武器弹药槽共享**（Ask3lad 式）：弹药列表=主炮+副炮；按炮上限；总量显示
14. **会话保存**：载具+地图/敌人配置自动保存（`session.txt`）
15. **ATGM 机炮打导弹**：FindGroundAmmoGroup 修复（单容器→组名）+ 主炮 blk 不无条件覆盖（注入/弹道才覆盖）+ 副武器 launcher 排除后撤销（Ask3lad 可配 ATGM）
16. **模块武器替换（BMP-1P Konkurs）**：moduleShipsWeapons 预检查 → 模块 commonWeapons 为基础
17. **T-80BVM 裸车额外 ERA**：`disableModEffects` 节点 → weaponPresets 预设 `hideNodes`（官方 CDK 机制）；`utl_stock_<id>.blk` 预设 + 任务 `weapons:t` 指向
18. **工具类改装件（维修/灭火/补员/火炮支援）**：确认 applyAllMods 与 modification 行均无效（任务单位不加载改装件系统）；改走**任务触发器**：按 F 键（ID_FLAPS）→ `unitRestore`（partRestore+ammoRestore+fuelRestore，不重生）+ `unitSetAttachedMenCount`（补员，无效）
19. **弹种选择**：CROSS-DOMAIN 区弹种下拉（`roundBox`）实时刷新（`Func<string,IList<GroundAmmo>> resolveCannonAmmo` 回调）；弹链组（belt）条目；选单弹裁剪 cannon / 选弹链组保留整组
20. **弹药量选项**：`UnlimitedAmmo` 复选框（9999/槽）→ missionAmmo.Count=9999
21. **弹药组名解析**：FindGroundAmmoGroup 弹链组（多弹）返回 projectile（修复 "projectile definition was not found"）
22. **换炮与弹药注入融合（CANNON 下拉，22:29 编译版）**：弹药面板顶部新增 CANNON 下拉（NATIVE 原生炮 + GROUND/NAVAL/AIRCRAFT/HELICOPTER 四域跨界炮），选炮后弹药列表刷新为该炮弹药（跨域炮走换炮逻辑，原生炮走现有槽逻辑）；弹药槽统一驱动弹种（跨域炮 mount 的弹药 SourceBlk=注入炮路径 → 生成时 missionAmmo 用弹药槽配置，不再只取第一个 bullet）；belt 组名条目解析为组内首个 projectile（ResolveAmmoSlotId）；弹药解析加缓存（选炮不卡）；跨域炮默认 9999 发；选炮时左右两侧双向联动（syncingCannon 防递归）；原生炮模式下自动清空 InjectedCannon* 防止残留跨域弹种污染原生 cannon
23. **selftest 断言修复**："Respawning"/"Rearmed" 检查从全文 IndexOf 改为只扫 playHint 块名（模板正文含 "without respawning" 描述文字，旧断言误伤；本次确认 selftest 已全绿 exit=0）
24. **飞机对抗措施（chaff）修复（23:42 编译版）**：chaff 由改装件空模块解锁（countermeasures_launcher_chaff / _chaff_large / countermeasures_belt_pack，游戏硬编码识别），但 UTL 发射器发现只按文件路径判断（不含 with_chaff → flare-only）导致现代窗口无 CHAFF 滑块；新增 HasCountermeasureUpgradeModules(fm)（模块名含 countermeasure 或 group=countermeasures）→ 有则 AllowsChaff=true；生成链路原本完整（with_chaff/chaff_only 文件均存在于游戏数据，混合弹链按比例生成）
25. **飞机设置持久化（00:07 编译版）**：AircraftSettings（含 OverrideCountermeasures/FlareRounds/ChaffRounds/按发射器 loadouts）之前只在内存字典，重启即丢且静默回退默认（生成时 fm 完全不改=游戏里 98 发全 flare）；新增 aircraft_settings.txt（%LOCALAPPDATA%\UniversalTestLab\，每行 B64(vehicleId)\tB64(SerializeSettings)），LoadCatalogs 后 LoadAircraftSettings、WorkspaceSetSettings/ShowAircraftSettings 保存后 PersistAircraftSettings

## 3. 待办 / 未完成（新会话从这里继续）

> 2026-08-29 状态更新：第 8 条（Ask3lad 共用 us_m2a4）已解决（代理分离 ussr_t_26_1940，见第 0 节）；第 9/10 条（苏-30MKM）已修持久化与对抗措施量化（见第 0 节/第 2 节 24-25）。新增待办见下方 #11-15。

1. **敌方目标预设（抄 Ask3lad）**：Map 窗口加一键靶标预设（300m/600m/800m 坦克靶、移动靶、空中/直升机靶、海上靶）——**尚未做**
2. **融合版待验证**（22:29 编译版，进游戏验证）：
   - CANNON 下拉选跨界炮 → 弹药列表为该炮弹药 → mount 到槽 → 生成任务后：换炮生效（炮管/射速/弹药组换掉）、任务槽子弹与 HUD 弹种正确
   - belt 弹链组条目 mount 后任务槽写组内首个 projectile（ResolveAmmoSlotId）——游戏是否认
   - 跨域炮 9999 发（Weapon 块 bullets:i=9999 + 槽 Count）是否真正生效
   - 原生炮模式：CANNON 下拉选 NATIVE → 生成走旧槽逻辑，确认不再残留跨域炮影响
3. **火炮支援**：`useCustomSuperArtillery`（任务编辑器字段）方案曾被回滚（用户判断可能错）；CDK 有 `missionCustomArtillery` 动作（enabled 开关）——未定
4. **补充乘员**：unitSetAttachedMenCount 对坦克无效（CDK 无补员机制）——任务限制
5. **灭火**：unitRestore 部分恢复不灭火；fullRestore 也不行——任务限制（CDK 无灭火动作）
6. **HUD 弹药显示**：注入炮 HUD 不显示弹药量（曾有修复：bullets0 组名——需回归验证）
7. **M61A1+B28 组合武器重量**显示 1308t（特殊方案，未处理）
8. **Ask3lad 冲突**：与 UTL 共用 `us_m2a4.blk`（后运行者覆盖）——测试时不要同时跑
9. **苏-30MKM 对抗措施数字错误（用户反馈）**：用户设 18 flare/80 chaff，进战斗数字错——根因是生成时 settings.OverrideCountermeasures=false（设置未持久化/未保存），fm 对抗措施块原样（flare-only 98 发）；已修持久化（#25），需重新设置+生成验证
10. **苏-30MKM 武器选择界面开不出来（用户反馈）**：catalog 数据完整（12 挂架/205 武器）、生成的 fm 结构与原版一致、loadout 引用的挂载名（r77_slot6→slot6、r77_fuse_slot6→slot7、gsh_301_common→slot0）全部有效——未定位到具体原因，需用户澄清是 UTL 界面还是游戏内界面、具体操作
11. **belt_type_limit=2 未实现**：changeBulletTypeForSet:b=true 的车（BMP-2M/PGZ）应允许同武器 2 种弹链，目前统一按 1（同发射器互斥）
12. **Pbv301/Bosvark belt_type_limit=3**：Ask3lad 例外值，机制不明，未采用（默认 1）
13. **导弹架数规则待通用验证**：12 架规则目前从"该车所有导弹武器 bullets:i 最大值"推导（铠甲SM-SV=12）；山毛榉（9A317M TELAR，架数可能不同）等需实测；TKB 类容器弹（4 发/架）按 BulletName 含容器名核心匹配
14. **弹药方案数据（vehicle_ammo.tsv 思路）未落地**：参考 Ask3lad json 结构（ammo/ammo_loadouts/ammo_limits/belt_size/belt_type_limit），路线 B 自产数据文件——当前为生成时实时提取（挂载名/发架/链数），数据文件化可后续做
15. **ERA 预设单位 ID 校验**：era_presets.tsv 的 ID 已从 catalog 验证；游戏更新后新车/改名需重新校验（Quick-GroundAmmo/Build-Catalog 重跑）

## 4. 关键机制笔记（避免重新摸索）

- **任务单位（tankModels）不加载改装件系统**：applyAllMods 只应用性能类；武器替换（commonWeapons effect）、工具类（engine 按名识别）都需显式处理（代理写 commonWeapons / weaponPresets hideNodes / 触发器）
- **hideNodes 必须写在 weaponPresets 预设文件**（CDK 机制），代理根级无效；"移除节点也从损伤模型移除"
- **任务触发器**：`playerShortcutPressed{control:t="ID_FLAPS"}` + `unitRestore{target_marking:i=0, target:t="You", partRestore/ammoRestore/fuelRestore}`；`enableAfterComplete:b=yes`；periodicEvent 0.1-0.25s；unitRestore 需要 `target_marking:i=0`
- **弹药槽（bullets0-3）**：写组名（普通组）或 projectile（弹链组）；游戏要求 projectile definition（弹链组名报错）；`ResolveAmmoSlotId(cannon, 子弹名或组名)` 统一解析（单弹组→组名，belt 组→组内首个 projectile，游离 bullet→"" 由调用方回退）
- **注入炮 missionAmmo**：跨域炮模式下任务槽 = 弹药槽配置（SourceBlk 匹配注入炮路径的 loadout），未配置时回退第一个 bullet（HUD）；UnlimitedAmmo 勾选 → 槽 Count=9999；cannon 文件始终写 bullets:i=9999
- **换炮融合 UI**：CANNON 下拉（左侧弹药面板）为唯一事实源；右侧 CROSS-DOMAIN 三级树仅作浏览/验证；双向联动用 syncingCannon 防递归；resolveCannonAmmo 结果按 NormalizeBlk 缓存（cannonAmmoCache）
- **坦克代理**（us_m2a4.blk）：include 原生单位 + 条件化 commonWeapons 覆盖（仅注入/弹道/模块武器时）；裸车时纯 include（DamageParts 原样）
- **模块武器替换**（如 BMP-1P）：UseAllModifications=true 时收集原生单位"空块改装件"（引擎识别）+ 有 commonWeapons effect 的模块 → 显式处理

## 5. 关键文件位置

- 任务：`<游戏>\UserMissions\Universal Test Lab\universal_test_lab_hot.blk`（HOT UTL）
- 代理：`<游戏>\content\pkg_local\gameData\units\tankModels\userVehicles\ussr_t_26_1940.blk`（2026-08-29 起与 Ask3lad 的 us_m2a4 分离）
- 自定义炮：`<游戏>\content\pkg_local\gameData\Weapons\groundModels_weapons\utl_ground\utl_ground_cannon.blk`
- 隐藏节点预设：`<游戏>\content\pkg_local\gameData\units\tankModels\weaponPresets\utl_stock_<id>.blk`
- 全局配置：`%LOCALAPPDATA%\UniversalTestLab\mission_options.txt`（+session.txt）
- CDK 动作定义：`<游戏>\WarThunderCDK\dagor_cdk\windows-x86_64\plugins\daEditorX\missions\actions\*.scheme.nut`（unitRestore/playerShortcutPressed/missionMarkAsRearmZone/missionCustomArtillery 等）
- 任务设置模式：同目录 `mission_settings.scheme.nut`（useCustomSuperArtillery 等）

## 6. 测试要点（每次改动后）

- 生成后进游戏前：重开 User Missions 标签（或重启游戏）清缓存
- 确认文件实际写入：hot.blk 的 weapons/applyAllMods/modification 行、us_m2a4 的 override 块、utl_ground_cannon 内容
- 回归项：BMP-1 8发导弹（4 Malyutka+4 Konkurs）、BTR-82AT 机炮不打导弹、T-80BVM 裸车一层 Relikt、按 F 维修补弹、注入 GAU-4/406mm、Mission Options 全局持久化
- 融合回归：CANNON 下拉原生↔跨域切换（列表刷新+右侧树联动）、跨域炮挂多弹种（任务槽>1）、belt 组条目、原生炮无残留注入、`--selftest` exit=0
