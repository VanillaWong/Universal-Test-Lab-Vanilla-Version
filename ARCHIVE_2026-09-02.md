# UTL 项目交接摘要（2026-09-02）

> 今日主题：**弹药数据链路闭环 + 游戏弹药机制破解 + 主页弹药面板重构 + 中英双语化**
> 前置阅读：ARCHIVE_2026-09-01.md（9 章）、ARCHIVE_2026-08-28.md（导弹架数规则）

## 一、游戏弹药机制研究成果（重要结论）

### 1. 任务层弹药槽规则（实测钉死）
- 任务层最多 **4 个槽**（bullets0-3 + bulletsCount0-3）
- **写容器名**（cannon 顶层块，如 `125mm_ussr_3BM42_APDS_FS`）；写 bulletName（`125mm_3bk_18m`）**游戏不认** → 回退成其他弹
- **空槽 + count**（`bullets0:t=""` + `bulletsCount0:i=N`）= **默认弹**（Ask3lad 式，BVM=3BK18M），可指定数量；这是"带默认弹"的唯一方式
- 全部空 → `count0=9999` → 游戏加载原生预设默认弹（userVehicles 代理继承 include 的预设，**不会 0 发**——归档 09-01 里"代理无原生弹药定义=0/38"的结论**已推翻**，0/38 只是 UTL 计数器显示）

### 2. "一辆坦克能打什么弹" = modifications 弹药包（beltOptions）
- **不是** cannon 容器（16式FPS 与 16式同炮 105mm_JSW，但 FPS 无 Type93 弹药包 → 打不了；16式有 → 能打）
- 弹药包 = tankModels `modifications` 里的**空块**，名字匹配某个 cannon 容器名
- **必须支持无口径前缀弹药包**（`USSR_APDS_FS`，BVM 的 3BM42 包）——`^\d+mm_` 前缀过滤会漏！Ask3lad DB 对照验证完全一致
- 弹药包名 ≠ 容器名时（USSR_APDS_FS vs 125mm_ussr_APDS_FS），**写入弹药包名**（Ask3lad 同款）

### 3. 弹链组打包（部分验证，blockCount 待实施）
- 游戏弹药槽（组）数 = `ceil(弹种数 ÷ Weapon块数)`（BVM1块=4槽、WZ-305双联2块=4种弹→2组、Wirbelwind4块=4种→1组、Puma1块但原生上限2）
- 原生上限：`bulletSetCount`（cannon blk，只有 2A38M=1）；Puma 无字段但客户端硬编码=2（MK30-2 双弹链）
- Weapon 块数 = tankModels 里同 blk 的 Weapon 块重复次数（Skink 4管但1块=1:1；Wirbelwind 4管4块）
- **blockCount 未实施**（Build-Catalog 还没加）——见待办

### 4. 弹夹守恒（无魔法常数）
- 链数 = 总发数 ÷ belt_size（每链发数）
- 弹夹（炮膛）发数 = blockCount × belt_size（WZ-305：2管×4发=8发；铠甲：4块×351=1404发，显示 1404/0）
- 显示 = 弹夹 X 发 / 备弹（写入链数 − blockCount）链；写入链数 = 炮膛 + 备弹（守恒）
- **配置链数 < blockCount 时**：游戏按弹夹容量装满（X/0），打完耗尽（游戏行为，非 bug）

## 二、弹药数据链路（全部完成并验证）

| 项 | 内容 |
|---|---|
| U1a | `ground_ammo.tsv` → **`ground_ammo.json`**（数组，含**容器列**；Build-Catalog.ps1 + Quick-GroundAmmo.ps1 同步；build.rsp 资源行改 json；LoadCatalogs/selftest/ModernShell 读取改 json） |
| U1b | `vehicle_weapons.json` 的 `beltOptions` 字符串数组 → **对象数组** `{name, calibre, rounds:[{bulletName, display, kind, mass, speed, explosive, caliber, penetration}]}`（Phase5 从 ground_ammo.json 按 source+container 匹配 rounds） |
| U1c | 窗口/面板过滤 = `同炮 && 容器 ∈ 车辆 beltOptions`（`ContainerAllowed`）；主页组选项显示 rounds 具体弹名；GroundFindEntry 匹配 Display；configure 槽位加 **CLEAR（×）** 按钮 |
| 无前缀包 | beltOptions 提取放宽：`^\d+mm_` 前缀 或 名字 ∈ cannon 容器集合（`USSR_APDS_FS` 找回，BVM 4 个弹药包与 Ask3lad DB 一致） |
| calibre | 无前缀弹药包从 cannon 容器反查口径（USSR_APDS_FS ↔ 125mm_ussr_APDS_FS 去前缀匹配），rounds 也按去前缀容器匹配（显示 3BM42-2） |
| STOCK | 空槽 + count = 默认弹（ResolveGroundMissionAmmo 保留 BulletName 空的槽；ConfigureGroundPlayer 写 `""+count`；解包循环跳过 `stock:` 前缀） |
| 修复 | `UpdateAmmoPoolText` 硬编码 4 槽 → BVM 3 槽越界（改用 Count）；selftest 检查紧凑 json 格式 |

## 三、主页弹药面板（Ask3lad 式，重写于 U1d 后再次重构）

```
GROUND VEHICLE LAB • 车名（标题已删，省位子）
    CANNON: 125 mm • 38 rds total（只读行）
    SLOT 1 [STOCK/3BM42-2/HE/ATGM/3BM60 ▾] [数量]
    SLOT 2 [▾] [数量]
    SLOT 3 [▾] [数量]
    SLOT 4 [▾] [数量]
    AMMO: 已用/上限（每口径池：30mm:0/2 belts |152mm:8/8）
    [CLEAR ALL] [APPLY TO MISSION]
```

- **下拉直接选弹**（无 MOUNT），选项 = STOCK + 该车 beltOptions rounds（带类型）
- **自动填数量**（Ask3lad 式：数量为 0 时填剩余池预算；池 = 每口径，belt 换算链）
- **LostFocus 只 clamp 当前槽**（所有槽合计 ≤ 总量，不削减其他已配槽）
- **计数器每口径池显示**（跳过次要武器：机枪/烟雾——按"有 beltOptions 或 missiles 的口径"判断）
- 数据流：`GroundAmmoLoadouts` + `GroundLoadoutCalibre`（stock:125 正确归口径）+ `TrimGroundLoadouts`（已停用，改为 LostFocus clamp）
- 主页**不再使用** GroundConfigurePanel（EXPERIMENTAL 完整版不受影响）

## 四、中英可切换（ModernText 本地化）

- `ModernText` 静态类：`Chinese` 开关 + `L(英文, 中文)` + `XamlMap`（XAML 静态文本映射）
- **所有 UI 文本包 `L()`**（主窗口代码 + 面板 + 预设/关于窗口 + 对话框，约 300+ 处）
- **XAML 静态文本**（Tab 按钮/标题栏/标签，40 项）：`ApplyXamlLocalization()` 构建后遍历视觉树替换（切换重建时重执行）
- **切换入口**：OPTIONS Tab → `界面语言: [中文] [English]`，点击 → "切换中…" 反馈 → 重建窗口（卡顿为重建开销，已加反馈）
- **持久化**：config.json `language` 字段（默认中文）
- **黑名单（不可动）**：selftest 比较串、车辆/弹药数据名、blk 路径、挂载名、改造名、配置键、嵌入资源名（UTL.xxx）、URL

## 五、汉化踩坑（重要）

1. **占位串比较被汉化破坏**：controller（UniversalTestLab.cs L3646/3648）用英文串 `"All Weapon Types"/"All Nations"` 判断"不过滤"——主窗口汉化下拉项后 SelectedItem 变中文 → 被当真实分类 → **武器列表全空**。修复：主窗口 `SelectedIndex > 0 ? SelectedItem as string : null`（占位项传 null，与飞机列表同款）
2. 所有传给 controller 的筛选值，**占位项必须传 null**（不能传汉化后的显示文本）

## 六、待办

1. **U1e**：移除 GROUND CONFIGURE 按钮窗口（ModernGroundConfigureWindow + 按钮 L2822；功能已分流：主页=日常弹药、EXPERIMENTAL 面板=换炮实验室）
2. **弹链 blockCount 实施**：Build-Catalog 记录同 blk Weapon 块数 → 写盘按 `ceil(弹种数÷blockCount)` 分组（WZ-305 双联不再白配；弹夹容量 = blockCount×belt_size）
3. **主页弹药预设 SAVE/LOAD**（Ask3lad 式，ammo_loadouts.tsv 已有旧逻辑可参考）
4. 汉化扫尾：用户实测遗漏的英文（ToolTip 等）
5. XAML Tab 占位文本（TabOptionsContent 等）代码重建后已覆盖，无需处理
6. `data/ground_ammo.tsv` 旧备份确认后可删

## 七、踩坑铁律（追加）

1. 汉化下拉占位项 → 传 controller 必须 `SelectedIndex>0 ? SelectedItem : null`
2. 任务层写弹药**只能写容器名**（或空槽=默认弹）；写 bulletName 游戏回退
3. `startsWith("stock:")` 的 SourceBlk 必须跳过解包（`stock:125` 不是真实路径）
4. 语言切换 = 重建窗口（ModernText.Chinese 在构造读取 config）

## 八、SARH→伪ARH 转换研究（2026-09-02 追加，同日）

> 起因：SANTAL 换炮注入 F-4 麻雀（AIM-7E-2），导弹“抽风”不可用；最终做成“伪ARH”转换并固化进 UTL。全程实测钉死。

### 1. 机制结论（游戏数据实证）
- **SARH 导弹正常工作的硬件前提**：载具雷达 blk 必须有 `illuminationTransmitter` 块 + `fsms.main` 里 `illumination` 状态机（监听 **`sarhMissileLaunch`** 事件 → setIlluminationTimeOut 120s + checkIlluminationTimeOut）。全游戏 10 个传感器 blk 里**只有爱国者 AN/MPQ-53 有**（grep 实证）→ 玩家车（SANTAL Rodeo-2 / 2S6 1RL144 / 道尔 9K330 / 铠甲 1RS1-4 / Buk viking）全部没有 → 注入 SARH 弹后零照射，“抽风”是物理必然，不是 UTL bug
- **radarSeeker `active:b = true` = 主动雷达（ARH）**：导弹自带发射机自照射，装到任何能锁定的车都能打（Fakour-90/AIM-54C 实测）；SARH 无 active 字段
- **band 匹配参考**：导弹 seeker band vs 雷达 track band（9M317M=9 ↔ 2S6/道尔 track=9；麻雀/9M38/MIM-104=8，玩家车无 track=8）；`illuminationTransmitter` 无 band 字段，照射是否查 band 未验证

### 2. 伪ARH 转换配方（v9 实测通过：TWS 直射 + 每 0.5s 一发 + 麻雀音）
```
1. radarSeeker 加  active:b = true                → 自照射
2. guidance 加   permanentlyActivated:b = true     → TWS 直射（免发射前锁定）
3. guidance 加   lockDistance:r = 16000 / inertialNavigation:b = true / useTargetVel:b = true
4. guidance     breakLockMaxTime 5 → 160
5. radarSeeker  lockAngleMax→60 / angleMax→60 / rateMax→20 / angleGateRate:r = 30
6. radarSeeker  加 distGate 块（filterAlpha 0.8 / filterBetta 0.05 / searchRange 5000）
7. 头部        shotFreq:r → 2（防连发撞弹；已验证值）
```
对 9M38 / 9M317M / MIM-104 / 霍克 等任意 SARH 弹通用。

### 3. 关键技术修正（推翻之前的错误结论）
- **rocketGun（飞机火箭弹）结构读 `shotFreq`**：铁证 = 游戏原生 UCAV 武器 `ucav_agm.blk`（groundModels_weapons 目录、rocketGun:b=yes、sound=weapon.rocketgun_132、shotFreq=20）——地面单位用飞机结构是官方做法。v3 机关枪是 1000.25 太大，不是结构不读
- **音效**：飞机结构（rocketGun）sound-only 事件触发（AIM-54A/Fakour-90/麻雀原版全是 `weapon.rocketgun_132`）；地面结构（cannon）需要 `sound_path`（legacy bank）+ sound_pathStudio + bank 内事件——`weapon.rocketgun_132` 不在坦克 atgm bank（`tanks/weapon_tanks/atgm`）→ 地面结构放麻雀事件必无声。**结论：要麻雀音必须保持飞机结构**
- **击杀显示**：地面结构走 bullet.bulletName 本地化名（显示“格斗麻雀”）；飞机结构走通用导弹图标（AIM-54 同样只有符号）——结构决定，非缺陷
- 地面结构转换（cannon + bullet{sam_tank + rocket}）能限速但丢音效，**已弃用**（v4-v8 全部路线）

### 4. UTL 功能（已实现，编译通过）
- EXPERIMENTAL 面板（GroundConfigurePanel）新增 **“伪ARH转换”CheckBox**（中文：伪ARH转换（半主动弹自主制导，TWS直射），ToolTip 含配方说明）
- 数据流：`AircraftSettings.FakeArhConversion` → `MissionSettings.Current`（持久化键 `fake_arh_conversion`，mission_options.txt）→ `BuildCustomGroundVehicle` 注入 cannon 时 `ApplyFakeArhPatch(cannon, 2.0)`（UniversalTestLab.cs，round selection 之后、WriteBytes 之前）
- `ApplyFakeArhPatch`：仅对 `guidanceType = "radar"` 且无 `active:b` 的武器生效；已转换（有 active + permanentlyActivated）幂等跳过；行级插入，缺啥补啥，不重复打补丁
- 面板构造时从 MissionSettings 恢复上次开关状态（与 InjectedCannonBlk 同款逻辑）

### 5. 测试要点
- 注入麻雀（或任何 SARH 弹）+ 勾选开关 → 重新生成任务 → TWS 直射、0.5s 一发、麻雀呼呼声
- 不勾选 = 原样注入（SARH 抽风是游戏正常行为，非 bug）

### 6. 待办（追加）
- shotFreq 目前硬编码 2.0（0.5s/发）；如需可配置化，加 settings 字段 + 面板数值框
- 雷达照射补丁（真·SARH 体验）路线未做：给宿主雷达 blk 加 illuminationTransmitter + illumination fsm（模板在对话记录），需新增 sensors 注入能力
- 归档 09-02 弹药机制章节结论均不受影响

## 九、分体防空部署机制 + 发射车改造（2026-09-02 追加，当日晚些）

> 起因：玩家分体防空（IRIS-T SLM / 03式 / 紫菀 / NASAMS 等）的发射车吃不到 rearm、换不了弹。最终把 IRIS-T TEL 改造成“独立作战 + 打伪ARH麻雀”的完整自行 SAM。全程实测钉死。

### 1. 分体防空 = slave（从属单位）系统
- FCS（雷达车）blk 的 `supportPlanes { plane { class:t = "tankModels/<发射车>" isSlave:b = true count:i = 1 } ×2 }` ——WT 复用“飞机支援单位”框架定义地面 slave 车；每 FCS 带 2 个发射车槽
- 9 套玩家分体防空：fr_samp_t / it_samp_t（紫菀）、germ_iris_slm（IRIS-T SLM）、jp_type_03_chusam、jp_type_81_tansam、sw_nasams、us_nasams、uk_sky_sabre、ussr_buk_m3（全是 fcs 定义 launcher 为 slave）
- 发射车（slave）侧配套：radio_station（接收数据链）+ antenna_target_tagging（目标指示）+ radarDeployTime（接收天线竖起）→ 玩家开 FCS 部署发射车，FCS 锁定 → 数据链 → 发射车发射
- **爱国者/S300 不在 slave 列表**：AI 单位（us_ai_/ussr_ai_）走 onRadarAs + expClass（AI 配对），且爱国者雷达/发射架是 1 马力拖车（horsePowers:r=1 不能开）→ 玩家玩不了，无解

### 2. 部署机制边界（实测钉死）
- supportPlanes 的 **plane 数量 = 部署数量，无硬上限**（改 3 台成功）
- **`@override:supportPlanes` 在 include 壳代理里被游戏忽略**（2 台不变）——必须**全文复制 FCS blk 到 userVehicles 代理再改**（教程式全文代理）
- slave 的 class **可指向任意原生 tankModel 类**（部署出 T-90A 坦克成功）
- slave 的 class **不认 userVehicles 代理文件**（userVehicles/xxx 部署不出来）——slave 从原生单位注册表（vromfs）加载

### 3. rearm（n 秒恢复弹药）不吃发射车的真根因
- `rearmTimeOnField` = unitSetProperties `object:"You"`（只作用**玩家单位**）；slave 发射车不是 You → 不吃 → 打完不恢复（不是“发射架弹药不吃装填”的问题）
- slave 单位弹药 = **武器 blk 自带**，不吃预设/任务弹药槽（部署的 T-90A：主炮空（走任务槽）但机枪有弹（武器自带）；发射车 8 发在架同理）
- 想给 slave 补弹：需任务触发器 targeting slave 单位（未验证方向）

### 4. 发射车为什么必须配雷达车 = 缺火控
- TEL 原生无 `sensors` 顶层块、无 `antiAirComplexMenu`（只有接收天线 + default_tank_spg 瞄准 + radar_drive 天线动画）
- 玩家单开 TEL：能开但无雷达/火控界面 → 无法锁定 → 打不出

### 5. 发射车改造配方（已验证：IRIS-T TEL 独立作战 + 打伪ARH 麻雀，正常节奏）
```
1. 全文复制发射车 blk → userVehicles 代理（玩家车）
2. 加 sensors：sensor{blk = 配对 FCS 的雷达（TEL 用 de_hensoldt_trml_4b）,
   turretIndex:-1, designationFromBody:true, designationTurretIndex:-1,
   dmPart = antenna_target_location_dm / electronic_equipment_dm（必须真实存在）}
3. 加 antiAirComplexMenu（照抄配对 FCS：isVerticalViewAvailable/hasTargetList/hasVerticalView 等）
4. radarDeployTime 3 → 0（原生 >0 = 雷达默认收起不激活；0 = 常开，SANTAL 同款）
5. 换弹：Weapon2.blk:t → utl_ground/utl_ground_cannon.blk（伪ARH 麻雀注入炮）
```
- 结果：TEL 雷达常开 → SAM 界面锁目标 → 发射架（8管 bone_rocket_01~08）打伪ARH 麻雀，**不用部署 TADS、无乱锁、节奏正常**
- 发射车缺的就是火控（雷达 + SAM 界面）；player 车全文代理可自由改（雷达、武器引用全能换）
- 顺带解锁“发射车打自定义弹”——之前 slave 换弹死路，玩家车（代理）换弹是活路

### 6. 死路清单（避免重复踩坑）
- pkg_local **同名覆盖武器文件 → 无效**（游戏只读 vromfs）
- slave class → userVehicles 代理 → 部署不出
- @override:supportPlanes（include 壳）→ 无效，必须全文代理
- 发射车本体（vromfs）改不了；只有“玩家车 = 发射车全文代理”可改

### 7. 待办 / 可扩展
- 03式 / 紫菀 / NASAMS 发射车同配方改造（各自配对 FCS 的雷达 blk 不同）
- UTL 功能化：选发射车当玩家车 → 自动加装火控 + 可选换弹（伪ARH 开关联动）
- slave rearm：任务触发器 targeting slave（未验证）
- 部署任意原生 AI 车当靶子/火力（supportPlanes class 任意原生类已验证）
- **指挥权机制（实测追加）**：master 只要**自带火控**（雷达 + antiAirComplexMenu）就能指挥部署的 slave 发射车群——不限于原生 FCS。改装 TEL（TRML-4B 火控 + 伪ARH 麻雀）部署 2 台原生 IRIS-T TEL → 玩家锁定 → slave 跟着齐射 ✅。画面 = 玩家改装发射车（自定义弹）+ 部署原生发射车群（原装弹）混编火力网。部署发射车打自定义弹仍是死路（slave 原生类）

## 十、真 SARH 研究 + 武器移植三段式配方（2026-09-02 追加，当日晚间）

> 起因：想让地面车打真半主动弹（不改伪ARH）。Osa（黄蜂 band9 平台）+ R-24R（band9 SARH）最终打通。全程实测钉死，含大量死路。

### 1. 真 SARH 三段式配方（缺一不可，全部实测）
```
1. BAND 匹配（硬门槛，不可绕过）
   平台 track 雷达 band == 弹 seeker band
   ✅ Osa(band9 track) + R-24R(band9) = 能打（成功案例）
   ❌ Osa(band9) + Sedjil/麻雀(band8) = 抽风（Sedjil 反证：已加 designation+照射仍抽 → band 是独立硬门槛）
2. designationSourceType:t = "radar"（加在弹 radarSeeker 内）
   飞机弹移植地面必须声明"吃雷达目标指示"；MIM-23/MIM-104（地面 SARH 原生）自带；麻雀/R-24R/Sedjil 都没有 → 之前所有真 SARH 失败都卡这
3. illumination（平台 track 雷达补 hardware）
   illuminationTransmitter 块 + illumination fsm（监听 sarhMissileLaunch，照 APG-66J/MPQ-53 结构抄）
```

### 2. 可用组合（当前唯一能玩真 SARH 的路）
- **Osa-AKM（band9 track）+ R-24R（band9 SARH）**：Osa 原生 search(360) + track 补 illumination + R-24R 加 designationSourceType + shotFreq=2 ✅ 真半主动命中
- band8 弹（麻雀/Sedjil 135km）在地面玩家链路**锁死**：玩家车无 band8 照射雷达（全游戏带照射的只有爱国者 MPQ-53(band6)/飞机雷达(band8)——MPQ-53 是拖车不可开，飞机雷达移植多坑见下）
- 平台限制：需**机械扫描单锁** SAM 车（Osa ✓）；Tor 相控阵无单锁 ✗；红旗11 无转塔(TEL)难瞄 ✗

### 3. 飞机雷达移植教训（APG-66J 上 Osa/红旗11 全程记录）
- 飞机雷达 scanPatterns 的 **azimuthLimits ±60 只是上限，真实扇区由 width 决定**（width60=120°/30=60°/10=20°）——只改 limits 无用
- 飞机搜索密度低（barsCount 2-4）；地面 360 需高密（红旗11 原生 144 条/barHeight 2.5/period 1）
- 飞机雷达 fsms：BVR 锁有 checkDesignationType（只认 target/cue designation 类型）→ 地面 SAM 锁定被拒 → 失败路径改 bvrLock 仍难锁
- ACM 锁可锁但窄锥；给 acmLock.detect 加 designatedTargetSearch 可补 designation
- 飞机雷达装地面：designationFromBody=turret 指向问题、双雷达冲突（search+track 打架）——**结论：别移植飞机雷达，用原生地面雷达补照射**

### 4. 其他死路/弯路（防重踩）
- 照射补丁对不匹配 band 无效（band 是独立硬门槛）
- saclos 改造（R-24R guidanceType→saclos + 9M33 式 lineOfSightAutopilot）：Osa 能引导（"听话"）但 R-24R 大弹体（244kg/4.49m/推力5万）蛇形弹道；限速 maxSpeed=580 仍蛇形——saclos 参数为小弹调，大弹体不可用。saclos 弹需信标跟踪平台（Osa 有；Tor 相控阵无）
- Tor（相控阵）：多目标 TWS 无单目标硬锁 → SARH 无法用（SARH 需要锁定单目标持续照射）
- rocketGun 结构 + saclos：游戏里 saclos 弹全是地面结构（cannon+bullet{sam_tank}）——飞机结构 saclos 兼容性存疑

### 5. 对照结论（弹种 vs 制导方式）
| 方式 | 配方 | 状态 |
|---|---|---|
| 伪ARH | active + permanentlyActivated + shotFreq | ✅ 稳定（任意车，任意弹，锁+射即中） |
| 真 SARH | band 匹配 + designationSourceType + illumination | ✅ 仅 band9 组合（Osa+R-24R） |
| saclos 改造 | guidanceType→saclos + 9M33 guidance | ⚠️ 能引导但大弹体蛇形 |

### 6. 待办 / 未来方向
- 找/造 band8 地面照射雷达平台（打通 band8 弹：Sedjil 135km/麻雀）——玩家车目前无解，除非改造带 illumination 的 band8 地面雷达（当前无现成，需深度缝合 MPQ-53 illumination + band8 地面雷达）
- UTL 功能化：真 SARH 注入（三段式补丁：designationSourceType + illumination 自动补 + band 提示）
- **Build-Catalog 跳 dummy 主武器**：**运行时逻辑已修（2026-09-02 收尾）**——BuildCustomGroundVehicle 主武器选择跳过 `dummy:b = true` 的 Weapon（优先非 dummy 的 gunner0，否则取第一个非 dummy Weapon——发射车真导弹 gunner1），新增 `IsDummyWeapon()` 辅助。现象（换炮塞进 dummy → S300 露观察窗/雷达）已修复并编译。Build-Catalog（ground.tsv MainWeaponBlk 仍记 dummy_weapon）数据层待下次重跑 Build-Catalog 时一并治。**注意坑：blk 的 bool 字段是裸值 `dummy:b = true`（无引号）——BlkTools.Field 只匹配带引号值（`:t`），判断 dummy 必须直接正则匹配 `dummy:b = (true|yes)`（踩过：第一版用 Field 返回 null → 修复无效）**
- **5V55（S300 弹）伪ARH 验证（收尾）**：508mm_s300ps_5v55（1664kg/75km，band8 SARH/TVM，双组弹）→ active + permanentlyActivated + breakLock160 + shotFreq2 → Osa 命中良好 ✅；伪ARH 对巨型弹同样有效
- **NASAMS 改装发射车实验（死路实锤，2026-09-02 深夜）**：目标：雷达车（us_nasams_fcs，alt+C 分体防空）召唤打伪ARH 爱国者的 slave。三路全试全失败：① pkg_local **同名覆盖原生 cannon/车 blk 无效**（vromfs 优先，只有新文件名如 utl_* 才生效——180mm IRIS-T 覆盖无效旧案同因）；② supportPlanes class → `userVehicles/xxx` 或 `tankModels/userVehicles/xxx` → **AI 无法部署**（slave 只认原生 tankModels 类，玩家代理路径 AI 不加载）；③ 原生类 + 任务玩家 bullets 改弹名（410mm_mim104b）→ 弹必须属于装置（cannon）bullet 列表，不属于则 fallback 副武器（打了 AIM-9X）。**结论：slave 只能原生类+原生弹，slave 弹由 FCS preset 链决定（FCS 引用的 cannon 装置）——改装代理作 slave = 引擎死路（九章结论最终实锤）**。想打自定义弹只能玩家车自己开火（Osa 模式：代理全文 + 武器 blk 引用 → utl_ground_cannon）
- **伪ARH 开关多组弹修复（收尾）**：ApplyFakeArhPatch 原只补第一个 guidance/radarSeeker 块——双组弹（5V55/MIM-23 等两组弹的文件）第二组仍是真 SARH（切弹后打不了）。已改为遍历全部块（InsertFieldInEveryBlock/R
- **S-75（V-759）Osa 一体达成 + inject-shell 原则（2026-09-03 凌晨追加）**：S-75 系统 = P-12 搜索（su_p_12ma，无 antiAirComplexMenu——AI 站触发器编组非 supportPlanes）+ SNR-75 跟踪（su_snr_75，纯 trk）+ SM-90 发射架（ussr_ai_sam_launcher_s_75，model s75m_launcher_sm90，单发轨 staticElevation 4.8°/bullets 10/无 ammoStowages/无 controlDevices）+ V-759 弹（654mm launcher，2397.9kg/60km，**saclos + beaconBand-1 + designationSourceType radar**——与 Tor 9M331 制导同族）。**关键教训：改装车换弹必须 inject-shell（保留车原生 cannon 发射机构 + 只换内部 bullet{} 块）——整个 launcher 文件当炮注入只在发射机构兼容时有效（5V55 多管轨 OK；V-759 是 SM-90 固定单轨+deployed 动画+attackMin/MaxHeight 200/21000+isAam+price 全拒收无弹）**。配方：Osa 全文代理 + Weapon2 → utl_ground_cannon（=209mm_9M33A3 cannon + bullet 换 V_759 块，双组）+ 任务 preset 清空走 commonWeapons → V-759 能打 ✅。V-759 特性：飞得慢（2.4吨）、必须雷达锁定（designationSourceType radar——不锁直飞无引导；9M33 可无锁直射）。也尝试过：Tor 车（弹箱 ammo_turret_01-08 装不下 8.2m 弹）+ 改单管 emitterGenRange 1,1——均无弹（Tor 弹箱结构拒 V-759）；slave 编组（S-75 无内建 supportPlanes、三车分工链玩家撑不起）不现实。**S-75 整套雷达上 Osa 也验证 ✅**：Osa 代理 sensors 主组 search→su_p_12ma（P-12）+ track→su_snr_75（SNR-75，自带 saclos 指令发射器）→ 锁定/引导 V-759 正常（AI 副组 su_9k_33m3_search_ai/track_ai 保留）；回滚备份 .bak_9m33radar。**9K33/saclos 双发限制（实测）**：Osa 同目标同时引导上限 2 发——第 3 发发射时最早一发失导（不听话直飞）——V-759 走同链同样受限——同目标最多同时 2 发，需间隔/换目标
- **Catalog 全面 TSV→JSON + 零解包改造（深夜收尾）**：① `tools/tsv2json.js` 通用转换器（schema 表+类型推断，16 个 TSV→JSON，data/ 共 18 JSON/35MB）；② LoadCatalogs 12 个解析循环→`JsonRows<T>()`（12 个新 DTO，JavaScriptSerializer 先例 ground_ammo）；③ selftest 子串校验→DTO/LINQ 断言（Node 等价验证 23/23）；④ build.rsp 资源全 .json（0 TSV Embedded，exe 43→57.4MB）；⑤ 坑：aircraft.tsv 藏 8 列（Kind=Helicopter 在 maxLoad 后）、selftest 在 Program 类需 `MainForm.JsonRows` 限定、长段 edit_file 精确匹配失败→程序化行替换可靠；⑥ **换武器卡顿修复**：ExtractGameBlk 加零解包快路径——自动探测 exe 父目录的 universal_units_data/universal_weapons_data 预解包树（`FindPreExtractedTrees`，缓存一次），命中直接读文件免 wt_ext_cli；武器/单位 blk 全命中（sensors 不在树仍走 AppData 缓存兜底）；游戏更新后需重解包树（Ask3lad 同限制）

## 十三、游戏更新标准刷新流程（2026-09-03 归档）

> 9-01 游戏更新后 catalog 过期（空战武器 inject 不全根因）。全套刷新流程如下。

### 流程（更新后跑一遍）
1. **解包 5 树**（wt_ext_cli，AppData tools 版）：`unpack_vromf --input_dir_or_file <game>/aces.vromfs.bin --output_dir <项目根>/universal_xxx_data --format BlkText --folder gamedata/xxx --continue Quiet`
   - universal_game_data ← gamedata/flightmodels（13207 文件 ~47s）
   - universal_units_data ← gamedata/units（5344 ~21s）
   - universal_weapons_data ← gamedata/weapons（4406 ~30s）
   - universal_lang_data ← lang.vromfs.bin --folder lang（58 csv）
   - universal_char_data ← char.vromfs.bin --folder config/shop.blk
2. **Build-Catalog.ps1**（⚠️ 默认 `..\xxx` 假设脚本在子目录——项目根跑**必须带 5 个相对参数**）：`-FlightModelsRoot universal_game_data\...\flightmodels -UnitsRoot ... -LangRoot ... -WeaponsRoot ... -ShopPath universal_char_data\char.vromfs.bin_u\config\shop.blk`
   - 脚本有实时进度（Write-Output Phase 1/4→5/5 + 每 100 计数）——自己终端跑可见（管道捕获会被缓冲）
   - 跑完输出汇总行（Aircraft=/DonorMounts=/Weapons= 等）
3. **tsv2json.js**：`node tools/tsv2json.js`（16 TSV→JSON）
4. **编译**：`csc /lib:...WPF @build.rsp`（Embedded JSON 更新）

### 2026-09-01 更新后实测增量
Aircraft 1576→1577 / Presets 10618→10623 / Slots 31979→32005 / Pylons 5275→5288 / DonorMounts 29813→29837 / Modifications 51241→51258 / Weapons 2198（同数内容更新）
eplaceFieldInEveryBlock/RaiseFieldInEveryBlock/InsertInertialGuidanceInAllGuidanceBlocks/AddDistGateToEverySeeker + IndexOfBlock 支持 from），已编译

## 十一、波段修改实验 + 真 SARH vs 伪ARH 对照（2026-09-02 收尾追加）

> 在第十章基础上继续实测（Osa 平台）：改 band 可行性 + 伪ARH 与真 SARH 的命中差距。

### 1. 改 band 的结论（实测）
- **改导弹 band（8→9）**：抽风 ✗ —— band 不是纯匹配标量，导弹 seeker 的配套参数（dopplerSpeed/dopplerSpeedGate 等）按原 band 调校，只改 band 数字会失配
- **改雷达 band（track 9→8，改照射副本 utl_osa_track_illum 不影响原生）**：可行 ✓ —— 雷达 blk 里 band 只在 transivers，影响面小；改后原 native band8 弹（R-27ER）能正常制导（不再抽风）

### 2. 真 SARH 命中瓶颈 = 平台照射精度（R-27ER 对照实验）
- R-27ER（真 SARH：band8 匹配 + designationSourceType + illumination）：**能制导但打不中** —— Osa track 不是专用照射雷达，照射指向精度不足（远/机动目标脱靶）；R-24R 小弹近距能中说明距离越近越可行
- R-27ER（伪ARH：active + permanentlyActivated）：**精准命中** —— 主动自导完全绕开平台照射精度

**最终结论：伪ARH（active 自导）是地面车的终极解——任何弹打得好不好只取决于弹体性能（band/照射/锁定全绕过）；真 SARH 在地面车受平台照射精度硬限制（无专用照射雷达前意义有限）。**

### 3. 其他发现
- **9M317M（Buk 弹）实为 ARH（radarSeeker active:b=true）**，非 SARH——Buk slave 无照射也能命中之谜解开（自导）
- Sedjil（投石）：band8 远程 SARH（135km），性能一般
- R-27ER：band8 SARH 350kg/100km，伪ARH 化后 Osa 精准（强弹推荐）

### 4. 可玩组合（收尾时状态）
- Osa + R-27ER 伪ARH = 精准强弹（伪ARH 配方：active + permanentlyActivated + lockDistance 20000 + breakLock 160 + shotFreq 2）

## 十二、UTL 伪ARH 开关运作机制（2026-09-02 收尾归档）

> 数据流 + 补丁内容速查，读代码前先看这节。

### 数据流
EXPERIMENTAL 面板 CheckBox「伪ARH转换」→ AircraftSettings.FakeArhConversion → MissionSettings（fake_arh_conversion 持久化）→ BuildCustomGroundVehicle（生成任务时，写入 utl_ground_cannon.blk 前）→ ApplyFakeArhPatch(cannon, 2.0) → 游戏加载伪ARH 版。

### 触发条件（三重）
1. 正在换炮注入（useCustomCannon）+ 勾选开关
2. 武器 guidanceType:t = "radar"（SARH/ARH）
3. 尚未转换（无 active:b=true + permanentlyActivated）——幂等（saclos/光学弹不受影响）

### 补丁内容（对每个 guidance/radarSeeker 块——多组弹全打，2026 修复）
| 位置 | 加什么 | 作用 |
|---|---|---|
| 头部 | shotFreq >10 → 2 | 限射速防撞弹 |
| guidance | permanentlyActivated:b = true | 导引头永久激活 → TWS 直射免预锁 |
| guidance | lockDistance 16000 / inertialNavigation / useTargetVel / breakLockMaxTime→160 / inertialGuidance+datalink | ARH 中段配套 |
| radarSeeker | active:b = true | 导弹自照射 → 绕开载具照射+band 限制 |
| radarSeeker | angleGateRate 30 / 宽跟踪角 / distGate | ARH 追踪配套 |

### 核心原理
active = 导弹自带发射机（ARH）；permanentlyActivated = 免锁定 TWS 直射。二者把 SARH 弹变“伪主动弹”——地面车打任意雷达弹的通用钥匙（麻雀/R-27ER/5V55 均验证，双组弹已修）。代码入口：UniversalTestLab.cs ApplyFakeArhPatch + IsDummyWeapon 附近。



## 十四、雷达替换功能 + UX 规划（2026-09-03 第二天归档）

### 功能交付（全部编译）
1. **ModernPickerDialog 通用组件**：Ask3lad 式搜索选择器（即时过滤/Esc 清空/无匹配提示/自动聚焦/442 条目不卡）——所有长列表选择复用
2. **sensors catalog**：Build-Catalog 收集 442 传感器（id/显示名/band——sensors.tsv→json→Embedded）——雷达 Picker 数据源
3. **雷达替换功能**：AircraftSettings 加 RadarSearchBlk/RadarTrackBlk/RadarStripAiSensors；EXPERIMENTAL 面板加 CHANGE RADARS（双 Picker）+ RESET RADARS TO NATIVE + AI 组 strip；生成时 ApplyRadarSwapToProxy（@delete:sensors + 重建——换 search/track 位引用 + 删 _ai）；**自动启用 SAM 界面**（原生 antiAirComplexMenu available:b=false——红外车如箭10——换雷达时自动 @delete + 启用完整菜单）
4. **坑记录**：① build.rsp 资源行正则路径不匹配（sensors.json 没加上→Picker 0 items——改后要查 rsp 实际行）② DTO band int 遇空串 "" 反序列化整表失败→改 string ③ Picker 类插进 MapPanelState 内嵌套→移 namespace 级 ④ 长段 replace 遇 \r\n 行尾失效→行定位插 ⑤ build.rsp 曾丢 embed 化（全 data\ 顶层 pretty）
5. **雷达角色判定**：fsm 模式名可靠区分（name/fsm 含 "search"=搜索；含 "track"/"lock"/"acquisition"=跟踪）——442 可程序打标
6. **弹制导决定雷达需求框架**：active(ARH/TWS)→只要搜索雷达（红旗11=cn_hq_11_search band5 + FM-3000 active band8 原生架构——搜 5 指 8 可行）；SARH 真→需跟踪/照射；saclos→跟踪+指令。箭10（红外车——it_9a35 菜单 available:false + su_9s86 红外跟踪不给雷达弹数据）整炮换 FM-3000 有弹打不了→换 cn_hq_11_search + 启用菜单→**能打 ✅**；箭10 密闭 120mm 弹箱 inject-shell 拒 FM-3000(250mm) 但整炮（250mm_FM_3000_user_cannon user_cannon 原生 active 8发）行——user_cannon 整炮 vs rocket_launcher inject-shell 判定不同

### UX 规划（docs/UX_PLAN.md 归档）
模式层 L1（陆战/空战/直升机/海战灰）+ L2 功能页（现 5 视图）+ L3 EXPERIMENTAL 子页化；选择器 Picker 化 + 详情卡（车/弹/雷达 stat card 像游戏）；资讯数据扩展（sensors role/rangeMax）；**爆改状态管理（新增需求）**：默认打开干净（不自动套用上次爆改）、一键/分别归零、爆改预设保存加载。里程碑 M1-M6 待实施。

### 待办：玩家 MapPanel 崩溃诊断（未解决——挂起）
- 现象：玩家报 NullReferenceException at MapPanel..ctor（BuildTargetsTab）——本机无法复现（GUI 正常）
- 已做：crash logger 增强（写 %LOCALAPPDATA%\UniversalTestLab\crash.log + exe 旁 selftest_crash.log + OS/culture/lang）+ build.rsp /debug+（pdb 行号）——诊断版已交给用户等玩家反馈
- 待办：玩家 crash.log 到手后按行号定位（ModernShell.cs MapPanel ctor 内——状态/语言/分辨率相关嫌疑——参考 ModernText.Chinese/首次运行无 config 路径）

### UX M1/M2a/M2b-0 完成（2026-09-03 晚）
- sensors catalog 全参 9+1 列：id/display/band/role(search-track fsm 判定)/rangeMax/type(radar/irst/rwr)/fsm 能力集/weaponTargetsMax(数据链目标)/irst(红外通道)/domain(100 ground 引用判定 + 342 air——船不引 gamedata/sensors——海战域待 ships 机制)
- 频段字母映射：band-3 → IEEE 字母（样本 5→F 红旗11/8→I 1RS1-4/9→J HQ-17TT/10→K 1RS2-3 全中）——1-3 罕见兜底数字
- HQ-17 TT blk ↔ 游戏内构字段映射实证（type/name/transivers band+range/irstTrack/weaponTargetsMax/scanPatterns tws）
- UI 待做（M2b-1/2）：雷达区动态槽位（无雷达车禁用/单槽车只显一槽）+ 游戏式详情条（列表简洁——详情独立条）+ Picker 域过滤

### S-300 上车成功 + 弹容设置（2026-09-03 深夜）
- **要地 SAM 上车 #1 达成**：Osa + 508mm_s300ps_5v55（整炮 cannon:b=true 自带 508mm_5v55r 弹 1664kg/7.25m/mach5.5/radarSeeker+惯导+数据链）+ 5N64S(64N6 搜索 band5 180km) + 5N63S(30N6 跟踪 band8 180km 照射) + 伪ARH → **能打 ✅**
- S-300 系统 3 雷达（真实/游戏）：64N6 搜索 + 55ZH6(Nebo 反隐身搜索 band5) + 30N6 跟踪——Nebo = 第二搜索源（玩家 1 搜索+1 跟踪够打——Nebo 冗余待多槽 v2）
- 四套要地系统组件全就位（AI 单位 tankmodels + 弹 groundmodels_weapons + 雷达 catalog）：S-300(5p85se/5v55/5n63s/5n64s/55zh6)、S-75(已验)、爱国者(m192/410mm_mim104/an_mpq_50 搜/an_mpq_53 跟)、霍克(370mm_i_hawk/an_mpq_61)
- **弹容设置**：AircraftSettings.InjectedCannonRounds（>0 生成时改写注入 cannon bulletsCartridge）——面板换炮链"每次装填弹数(0=沿用原值)"——Osa+S-300 填 6 = 6 轨架满齐射

### S-300 上车 / 真半主动 / 预设系统（2026-09-04 上午归档）
- **S-300 上车**：Osa/整炮 5V55（508mm_s300ps——cannon:b 自带弹 1664kg）+ 5N64S 搜 + 5N63S 跟——能打 ✅；Buk-M3 launcher（VLS 垂直筒——同族视觉）+ 5V55 + 9S18M3 搜 + Viking 跟 = 整合式 S-300（伪ARH 打）
- **S-300 系统 3 雷达**：64N6 搜（5N64S band5 180km）+ 55ZH6 Nebo 搜（band5 反隐身冗余——玩家 1 搜 1 跟够）+ 30N6 跟（5N63S band8 180km illumination）
- **真半主动入口（重大）**：AI Buk 9A310（ussr_ai_sam_launcher_9a310）= 唯一玩家能开的自带照射车——9S35（band8 60km illumination）+ 9M38 半主动（seeker8 LOAL 惯导/数据链/breakLock 10）——Alt+F 锁/Alt+X 导引头/左键发射——**需持续锁着才中**（真 SARH 玩法）；9A310 无 controlDevices（不能移动——固定阵地打）+ 无 SAM 菜单（键位操作）；能开 = 车结构完整（乘员/座舱/真 cannon 自带弹/传感器）——S-75 静态架不行（无武器/座舱）
- **全 SARH 弹 seeker = band8**（9M38/5V55/R-27ER/麻雀全系/Skyflash）——全部匹配 9S35 照射——9A310 万能真半主动平台（R-27ER 等直接上）
- **预设系统 v1**：data/ground_presets.json（Embedded——9 预设——一键装配按钮（EXPERIMENTAL header——Picker——自动切车+填全配置+重建面板）——坑：WorkspaceGetSettings 返回副本（必须 WorkspaceSetSettings 写回否则不生效）；预设空 cannon = 原生弹（需清 MissionSettings.Current 全局旧值否则被污染注入旧炮）
- **雷达加装 bug**：ApplyRadarSwapToProxy 只替换不新增——修复（缺槽用首 sensor 模板追加）；9A310 加 9S18 搜索（turret-1+location dm 标准挂法搜索列表 OK）但**干扰 9S35 锁定**（假锁无框/只东扇区）——9S18 移除（9S35 恢复）——单跟踪塔车加搜索 sensor 冲突教训（Tor 式 search+track 同 turret 0 双 dm 正常——9A310 跨塔冲突）
- **弹容设置**：InjectedCannonRounds（生成改写 bulletsCartridge）——Osa+S-300 填 6 = 6 轨齐射
- **Osa + 9S35 加装 + 9M38（6 发）**：复制 Buk 玩法到 Osa 平台（预设 osa_buk_9m38_sarh——native 9K33 搜 + 加装 9S35 跟/照 + 9M38 真半主动——伪ARH 关闭时 breakLock 10 脱锁失的）
