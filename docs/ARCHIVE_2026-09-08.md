# ARCHIVE 2026-09-08(更新版)— 换炮弹药体系(saga 进行中,有实质进展)

> 新会话开场:1) `git status`(工作区大量未提交改动,见 §3);
> 2) 读 §2(最新状态与验证点);3) 读 §4(用户压缩前拍板/待定事项)。

## 1. 一句话状态(2026-09-08 14:00)

换炮(KVT 105 ← 2A46M 125)的**弹药注册链已打通**:生成时代理 modifications 弹药包
从 105 换成 donor 容器全集(容器+_ammo_pack 双空块)、任务 modification enable 行
同步 105→125。**卡海已修**(曾因 donor 空块只写 `name {` 未闭合 → 代理 BLK 括号
失衡 → 整车加载失败)。已能进游戏、能打。
**当前遗留两个引擎实测结论(未最终解决)**:
- 全槽 9999 → 引擎只给第一款装弹(总装载有容量钳制)
- 弹药工作台被自动填槽逻辑"砍废"(用户不满)→ 已确认方向:工作台/主页
  (同一数据源 GroundAmmoLoadouts)为主控;自动逻辑只做"105→125 名字翻译+保底"
- 用户拍板:弹药量参考 = **原车**(cannon catalog 的 NativeAmmo,非指定某辆车);
  UI 形式(A 分配模式下拉/B 上限切换)未定,待下次选

## 2. 机制与实测(勿再考古)

- 引擎弹种列表/默认弹 = 车 weapons 预设 + modifications 弹药包(空块,容器名,
  每弹种 = 容器块 + `容器_ammo_pack` 一对);cannon 文件只是发射器+容器全集
- 任务层按 `modification:t="..."` 行 enable;行里引用的块若在车定义中不存在 → 无效
- **代理弹药包重写**(Generate.cs useCustomCannon 分支,已实现):
  - donorCannonContainers = CollectCannonContainers(换炮 cannon 全文)
  - 重建原生 modifications:剔除宿主弹药块(名 `^\d+mm_` / `_ammo_pack` 结尾,
    **按 Start 降序删,否则索引错位**),保留研究块
  - 注入每容器 + `_ammo_pack` 两个**完整空块 `name {}`**(曾写 `name {` 只开不闭=卡海)
  - `@delete:modifications` + 重定义输出(代理 L~940)
  - settings.EnabledModifications 同步:删宿主 105 ids,加 donor 容器+pack
    (GetAircraftSettings 是字典缓存,Generate 内改 → Actions configure 可见)
- 任务弹药槽(actions.cs cannon-swap 区):ForcePlayerAmmoSlots(新 helper,
  BlkTools,4 槽 × {容器,Count});Actions 现实现=玩家所选(chosen)槽0 +
  donor 其余容器按文件序补 1~3(去重,最多 4 槽),每槽 9999
  → **用户实测:全 9999 引擎只出第一款(槽0)** —— 需改定量(见 §4)

## 3. 工作区/提交状态(压缩时)

- 已推送:c1e2868(归档 09-08 首版)、此前 8fe774c/06f579c/787e859/1215bea
- **未提交(全部已编译+selftest 绿,dist exe 为最新)**:
  - UniversalTestLab.MainForm.Generate.cs:代理 mods 重建+容器采集+enable 同步
  - UniversalTestLab.BlkTools.cs:CollectCannonContainers/ResolveCannonContainer/
    ForcePlayerAmmoSlot/ForcePlayerAmmoSlots
  - UniversalTestLab.MainForm.Actions.cs:换炮弹药接线(force 槽+多槽队列)
  - 上三者在 1215bea 后陆续改;selftest 无换炮弹药断言(待补)
- commit 约定:不加 Co-authored-by trailer
- 实测产物:KVT 代理 `游戏根\content\pkg_local\gameData\units\tankModels\
  userVehicles\ussr_t_26_1940.blk`;热任务 `游戏根\UserMissions\Universal Test
  Lab\universal_test_lab_hot.blk`(weapons:t 仍 us_m1_abrams_kvt_default=105 预设,
  槽 0=玩家选弹 9999,modification 行=125 容器+pack 全 7)

## 4. 用户决策与待定(压缩前现场)

- ✅ "原车"定义:弹药量参考 = **cannon catalog 条目的 NativeAmmo**(该炮在原车的
  携带量;NativeAmmo>0 用之,<0→9999,0→回落宿主量)—— 不是"指定某辆具体车"
- ✅ 宿主车弹药量 = vehicle.MaxAmmo(工作台 AmmoCapacity 主来源)
- ❓ UI 形式待选:A. 分配模式下拉(自定义/按宿主架/按原车架,自动分配 4 槽,
  仍可手改);B. 仅每槽滑块上限在两值间切换
- ❌ 用户对 9999 全塞不满:"不能全部都 9999,不然只会出现第一个"(引擎容量钳制)
- ❌ 用户指出工作台被砍:自动逻辑只认工作台第一槽、无视 Count、列表源仍宿主 105
  → 修正方向已共识(未实施):逐槽映射(105→donor 容器)+尊重 Count+STOCK→donor 首
  容器+全空才保底(单槽 9999 曾验证可打);弹列表源统一(方案 C)未做
- ❌ "注入任意炮弹"toggle(injectionToggle,页1)= 只是列表过滤器(query.Concat(catalog)),
  不注册弹药;普通车选外部弹 → 槽引用车没有的容器 → 引擎忽略(没反应)——用户"算了",
  暂不做(非换炮车的外部弹注册需要把该容器也注进代理 mods,机制同换炮,将来可做)

## 5. 其它 UI 事实(回答"两界面是否一样"时确认的)

- 主页 4 槽与 EXPERIMENTAL 工作台读写**同一个 settings.GroundAmmoLoadouts**
  (4 槽×{弹,Count});两 UI 是同一配置的两个编辑器(主页简化,工作台高级:弹库
  搜索/原生预设加载/STOCK 选项)
- 工作台页 5 Tab:弹药工作台/换炮注入/雷达替换/弹药开关/载具数值;
  换炮 round 下拉(InjectedCannonRound)= 生成 cannon 嵌入用,≠ 工作台装载槽
- slot 数量滑块 Maximum 已按"弹所属炮 NativeAmmo"给上限(无数据回落宿主量)

## 6. 全局待办(未变)

1. 换炮弹药收尾(§4:定量多槽/工作台回归/UI A-B 选择/列表源统一 C)
2. 飞机换雷达(参照 ApplyRadarSwapToProxy;sensors catalog 442)
3. git 历史全清(去 co-author trailer;filter-branch+force push;先 bundle)
4. beta.8 发布(料:坦克复活/空中重生/出生点/dummy 手术/5SAM/NASAMS;
   建议等换炮 saga 收尾)
5. FCS(IRIS-T 等)复验

## 7. 会话经验(追加)

- 生成产物代理文件的括号平衡可用 PowerShell 逐行 `{`/`}` 检查;引擎加载失败
  ("对着海")先怀疑代理 BLK 失衡
- edit_file 大批量跨文件编辑时,同批 edits 全指向一个文件才安全;CRLF/LF 混合
  文件用单行锚点避开行尾问题
- 用户对"自动决定弹药配置"敏感:先问清语义(工作台为主/9999 总量问题)再改;
  用户实测发现优先于代码推测(引擎容量钳制 9999 行为)
