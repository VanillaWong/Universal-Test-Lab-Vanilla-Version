# Universal Test Lab — 修改归档（2026-09-01）

> 本文件记录 2026-09-01 会话（Clean Test Range 防空系统 + 空射 SAM 导弹）全部改动。
> 新会话必读：结合 `ARCHIVE_2026-08-31.md`（含"三e"章节=此前最终状态）与本文件使用。

## 本次会话核心成果

### 1. Clean Test Range 分体防空系统（4 套，全开关可切换）

**四套系统**（复刻自 nuclear_escalation_tdm.blk 原版机制）：

| 系统 | 发射单元 | 火控 tracker | 搜索 detector | 预警雷达 |
|---|---|---|---|---|
| S-300 | 5P85SE ×4 | 5N63S | 5N64S | 55Zh6 |
| 霍克 | M192 ×6 | AN/MPQ-46 | CWAR | TPS-59（共享） |
| 爱国者 | M901 ×4 | AN/MPQ-53 | CWAR | TPS-59（共享） |
| 山毛榉 | 9A310 ×2 | 无（自带火控） | 9S18 | 无 |

**位置**（南北排开，间隔 400m，x=1235.74 跑道东侧）：
- S300 z=-35.66 / 爱国者 z=364.34 / 霍克 z=764.34 / 山毛榉 z=964.34
- 55Zh6 @(1635.74,30,-35.66)、TPS-59 @(1635.74,30,364.34)（各在东 400m）
- 参考：VIDAR @(665.74,29.85,564.34)；跑道 x=551.7，z=-74.9(北)→1175.1(南)

**分体防空能攻击的三个必要条件（血泪，排查顺序）**：
1. **mission 级集体探测配置**（模板 mission_settings.mission 已加，照抄原版）：
   `overrideMaxActiveUnitsWeight:i=600` / `alwaysSendAllyGMData:b=yes` / `isAlwaysShowCollectiveDetectedUnits:b=yes` / `useAIForCollectiveDetection:b=yes`（缺 → 雷达能扫到但数据传不到发射车）
2. **unitsSetSite 绑定**（模板 create_ctr_sites 触发器，照抄原版 create_sites）：把 `detector squad → tracker(单台) → fireUnit(发射车)` 显式绑定，缺 → RWR 只响扫描、永不锁定
3. **预警雷达**（S300 需 55Zh6、霍克/爱国者需 TPS-59，原版 spawn_longrange_radars 触发器）：缺 → 系统不完整
4. 山毛榉是例外：9A310 自带火控雷达，上述都不需要也能打
- 补充：unitSpawnOnObjectGroup 的 type 装配（launcher/tracker_radar/detector_radar → 同锚点 dyn_sam_site_wide_arragement）只是把单位摆上阵地，**不是**统属

**UI（MAP & SCENARIO → GROUND RANGE POSITIONS 下方 SAM SITES 行）**：
- 下拉：S300 / PATRIOT / HAWK / BUK / ALL（默认 S300，单选生成）
- 四态按钮循环：ACTIVE(红)=生成打玩家 / PASSIVE(黄)=生成不攻击(attack_type:dont_aim) / **FRIENDLY(青绿)=army 改1，拦截敌方靶机 Target_Air/Heli** / DISABLED(绿)=不生成
- 持久化：config session `sam_sites_mode` / `sam_sites_selection`（旧 `sam_sites` 自动迁移）
- 代码：`BlkTools.SetSamSites(text, mode, selection)`；friendly = 正则把 CTR_ 块 `army:i=2`→`1`（用 `${1}1` 避免 `$11` 歧义）；selftest 已覆盖三种模式

**模板触发器清单**（全部 is_enabled 由开关控制）：
`spawn_ctr_s300_sites` / `spawn_ctr_hawk_sites` / `spawn_ctr_patriot_sites` / `spawn_ctr_buk_sites` / `spawn_ctr_aew_55j6`（跟 S300）/ `spawn_ctr_aew_tps59`（跟 hawk|patriot）/ `create_ctr_sites`（含 ctr_s300/hawk/patriot/buk_sites 子触发器，unitsSetSite 绑定）

### 2. 空射 SAM 导弹（inject any weapon 新增 3 条）

`data\weapon_catalog.tsv` 新增（Ground SAM Missiles 类别，机制 `utl-sam:路径#bulletName`）：

| 导弹 | tsv 条目要点 | 质量 |
|---|---|---|
| 5V55R（S-300） | `508mm_s300ps_5v55_rocket_launcher.blk#508mm_5v55r` | 1664kg |
| MIM-104B（爱国者） | `410mm_patriot_mim_104_rocket_launcher.blk#410mm_mim104b` | 900kg |
| MIM-23B（霍克） | `370mm_i_hawk_rocket_launcher.blk#us_mim_23b` | 625kg |

- 已验证：米格-25 挂 03 式能打（Type03 原本就在列表）；三个新导弹能发射（可能无制导乱飞，radar 制导无地面数据链，实测为准）
- 机制：`PrepareInjectedWeapon` 解包游戏发射器 blk → 按 bulletName 提取 rocket 块 → 生成 `pkg_user\gameData\Weapons\rocketGuns\utl_sam\` 适配器

### 3. 踩坑记录（新）

- **selftest 测不到 GUI 构造**：MapPanel 构造函数里过早调 `UpdateReactionButtons()`（passiveShipBox 未初始化）→ NullReferenceException 启动崩溃，进程 36KB/0CPU 挂起特征。教训：改 UI 必须沙箱真实启动验证（工作集 300+MB=主窗口构建成功）
- **启动崩溃诊断**：Windows 事件日志（.NET Runtime 1026 + Application Error 1000）带异常堆栈，直接定位崩溃点
- Regex.Replace 替换模式 `$11` 有歧义（可能解析成组11）→ 用 `${1}1`
- selftest 里普通字符串写正则 `\s` 报 CS1009 → 用 `@"..."` verbatim
- exe 被占用 → csc CS0016 编译失败；僵尸 UTL 进程（管理员权限）杀不掉 → 管理员 PowerShell `taskkill /F /IM UniversalTestLab.exe`
- CWAR 雷达被辨认成"霍克雷达"：游戏数据绑定（`model:t="mim_23_hawk_radar_cwar"`），原版爱国者也用 CWAR 当搜索雷达，任务层无法改，属正常
- 玩家可玩 vs AI 版：AI 版 999 行/19KB（无 controlDevices、tank_crew 空、maxFwdSpeed=0），玩家版 77KB——AI 版只能试驾单载具，无法体验分体协同

### 4. 解包发现（wt_ext_cli，数据在 universal_*_data 目录）

- 官方玩家可玩分体防空 10 套（玩家开 FCS 火控车，supportPlanes+isSlave 机制，alt+C 部署发射车、可切换）：
  `ussr_buk_m3_fcs`(山毛榉-M3) / `germ_iris_slm_fcs`(IRIS-T SLM) / `jp_type_03_chusam_fcs`(03式) / `jp_type_81_tansam_fcs`(81式) / `fr_samp_t_fcs`+`it_samp_t_fcs`(SAMP/T) / `sw_nasams_fcs`+`us_nasams_fcs`(NASAMS) / `uk_sky_sabre_fcs`(天剑) / `us_sl_amraam_fcs`(SLAMRAAM)
- 9S18（山毛榉搜索雷达车）是 AI 版里最接近可玩的（有乘员/雷达/radarDeployTime/能开 75km/h，只缺 controlDevices 和 antiAirComplexMenu）——待整活方向

### 5. 备份清单（本次）

- `template\universal_test_lab.blk.bak_pre_sites`（加 SAM 阵地前）/ `.bak_pre_split`（触发器拆分前）/ `.bak_pre_aew`（预警雷达前）/ `.bak_pre_gmdata`（集体探测前）/ `.bak_pre_patriot`（霍克正名+爱国者前）
- `backup\universal_test_lab.blk.20260831_pre_sam`（最初加 SAM 前）
- `data\weapon_catalog.tsv.bak_pre_sam3`（tsv 加导弹前）

### 6. 下一步可能方向（未定）

- 9S18 雷达车可玩化实验（UTL 代理机制硬塞）
- S-400 / 40N6 等更多导弹加 tsv；官方 FCS 分体防空（Buk-M3/IRIS-T 等）摆进测试场当友军/敌人
- 四套系统 ALL 模式会穿模（同位置）——已南北排开 400m 解决；如要多套同场可继续拉开
- 发布 beta.5（含本次全部改动）

## 7. PvE 任务分析（pve_warfare_2077.blk / pve_container_port.blk，官方 DOM 防守模式模板）

> 源文件在对话附件（2077: 540.8KB/21429行；container_port: 432.6KB/17091行，全 CRLF），未存入项目。

### 7.1 TankAI 系统（AI 战车行为，最值得学）

**TI_init**（unitSetProperties 批量 AI 参数，可直接复用给 UTL 的 AI 目标）：
- 精度：accuracy:r=6 / maxDeviationAngle:r=0.3；对玩家射速加成 effShootingRateByPlayer:r=1.35
- enemyFireEvasion:t="chaser"（被射击会规避）、isFieldRepairEnabled:b=yes（AI 会维修）
- aiGunnersEnabled / aiGunnersDistance:3200（AI 机枪手自动打）、useSearchRadar:b=yes
- stealthRadius:6250 / hideProb:36 / calmDetection、slowWhenEnemyNear:0.7 / minDistBetween:10
- advancedCollisionAvoidance:b=yes、allowOvertakeMode:b=yes

**AI 反应三件套**（被打→反击/烟雾/炮击）：
- WhenHit：unitWhenHit 检测 → AI_HitClock++ → unitAttackTarget{attack_type:"attack_target", maxNumAttackersPerTarget:5} 全队反击；unitGetRandomNameFromSquad 随机抽单位、unitSetVar(AI_Last_Offender) 记攻击者
- TI_QuickDeploySmoke：AI_HitClock>2 → unitSetProperties(smokeScreenActive=yes) + unitDropAmmo{triggerGroup:"smoke"} + unitModifyVar(-1)
- TI_RequestArtillery：missionUnitShootAtLine{areaNameFrom/To, unitName, shotCount, attackLineRange:0.76~1, heightRange:600~650, lateralOffset:50} 区域弹道轰炸；炮击单位 unitSetProperties(isImmortal/stealthRadius:-1/calmDetection)

**Movement 状态机**（1683 行）：9 种行为模式（1-2 随机占领区/3 近占领区/4 远敌/5 敌出生点/6 占领区偏移/7 跟随友军/8 巡逻/9 集结重生）；TI_Check_NM 每 0.6 秒 varSetRandomInt(1-9) 随机选模式 → triggerActivate；unitMoveTo{navmesh, speed:90, distributionRadius:15, teleportHeightValue:1000}（传送高度防卡）；duplicateAreaAtPos 动态生成目标点；T1_ResetStuck 17.5 秒检测卡住重寻路

**波次重生**：unitWhenStatus{isKilled} → unitWakeUp + enableAfterComplete:yes + triggerEnable 循环；varSetRandomInt 随机队伍/重生点（TI_RandomRespawnDead）；TI_RRD_Team1/2 = 出生区 isTeamNeutral → unitSetProperties(army=1/2) 分队伍

**变量系统**：@bots_squad 等 @前缀引用；unitGetRandomNameFromSquad / unitSetVar / unitModifyVar / varSetRandomInt(check_prev_value 防重复) / variables{} 全局变量块

### 7.2 占领区域机制（回答"占领区维修补给"）

**三件套**：
1. objectGroups{ unit_class:"capture_zone_circle", army:0 }（cz_circle_01~03）
2. dom_start 触发器里 missionMarkAsCaptureZone{ target:@zone_XX_name, zoneType:"capture", iconIndex:3~5, canCaptureInAir:no/canCaptureOnGround:yes, onlyPlayersCanCapture:no, timeMultiplier:1.5, markUnits:"cz_circle_01", showBorderOnMap:no, captureNoPenalty:yes, zoneDefenders{}, tags{直升机/坦克} }
3. variables 里 zone_01_name 等区域名变量（@zone_01_name 引用）

**维修/补给结论**：这两个任务**没有**"占领→补给"显式触发器——补给用 restore_ammo（无条件周期 unitRestore{ammoRestore:yes, partRestore:no, supportPlanesRestore:yes}，默认 is_enabled:no）；rearmZoneName 变量仅预留未消费；**占领区维修补给是引擎内置行为**（capture zone 占领后归己方，靠近自动维修补弹）。
- unitRestore 参数：ressurectIfDead / fullRestore / ammoRestore / partRestore / supportPlanesRestore / power
- 注意：文件里没有 missionMarkAsRearmZone（交接文档里那个"补给区替代 addAirfield"的机制这两个任务没用）

### 7.3 对 UTL 可复用清单（整活方向）

1. AI 目标升级：Target_03 等加 enemyFireEvasion + 被打反击 + 放烟雾 + 呼叫炮击（unitWhenHit / AI_HitClock / missionUnitShootAtLine）
2. 区域炮击测试：missionUnitShootAtLine 指定单位打指定区域
3. AI 巡逻推进：unitMoveTo navmesh（现在 UTL 的 AI 基本站桩）
4. 波次重生：unitWhenStatus isKilled + unitWakeUp + triggerEnable 循环
5. 占领区测试：capture_zone_circle + missionMarkAsCaptureZone 在测试场放点，体验引擎内置维修补给
6. AI 参数：TI_init 那批 unitSetProperties 字段（accuracy / effShootingRateByPlayer / enemyFireEvasion 等）

---

## 8. 占领区（Capture Zone）——已落地成功（2026-09-01 晚）

**位置**：`(900,30,700)` 跑道东约 350m（可调），Sphere 半径 80

**配置**（模板 `template\universal_test_lab.blk`，备份 `.bak_pre_capturezone`）：
- area：`ctr_capture_zone`（Sphere，`tm:m=[[80,0,0][0,80,0][0,0,80] [900,30,700]]`）
- objectGroups：`ctr_cz_circle`（unit_class=`capture_zone_circle`，army=0 中立）
- 触发器 `spawn_ctr_capture_zone`：initMission → missionMarkAsCaptureZone
- **重要：触发器头部 is_enabled 和 comments 必须分行**（`is_enabled:b=yes comments:t=""` 同行 = FATAL ERROR，坑1 重演过一次）

**成功的关键（血泪，缺一不可）**：
1. **mission 级字段**（缺了 = 标记了但进圈无进度条）：
   gt_mp_capture:b=yes / gt_cooperative:b=yes / coop_mode:i=1 / coop:b=yes / hasTeamDesignation:b=yes / isTanksAllowed:b=yes
   （照抄 pve_warfare_2077 的 mission_settings.mission；原版 type=teamBattle，UTL 保持 singleMission 也能工作）
2. **tags 配载具**：坦克用 `tank:b=yes`（air:b=no）；canCaptureOnGround:b=yes、canCaptureInAir:b=no
3. **维修/补给 = 引擎内置**：占领后归己方，靠近自动补，无需触发器（restore_ammo 只是无条件周期补给，与占领无关）

**验证**：坦克进圈 → 进度条 → 占领 → 引擎维修补给 ✓

**hot blk 教训**：改模板后游戏加载的是 UTL 生成的 `universal_test_lab_hot.blk`——必须**重编译 + 跑新 exe 重新生成任务**，否则旧 hot blk 带旧 bug（曾因此 FATAL ERROR 两次）。

## 9. 坦克弹药量问题（调查中，未完全解决）

**用户现象**：选 T-80BVM + 弹种（3BM60）+ 输入数量 → 游戏仍显示默认弹（38 发）或 0/38

**机制（ConfigureGroundPlayer / BuildCustomGroundVehicle）**：
- 无配置：`bullets0-3:t=""` + `bulletsCount0:i=9999`（游戏用原生默认；**对 userVehicles 代理类 = 0 发**——代理无原生弹药定义）
- 有配置：`bulletsN:t=AmmoGroup ?? BulletName` + `bulletsCountN:i=Count`（Count>0 才被 ResolveGroundMissionAmmo 接受）
- **已改未编译**：无配置时 fallback 第一个 bullet + 9999（代理类也有弹）——代码已改（UniversalTestLab.cs），编译被 exe 占用挡过一次，**需重新编译**
- selftest 断言写入 AmmoGroup（容器名如 120mm_us_M829A3_APDSFS）；T-80BVM 走 BulletName（125mm_3bm60）

**三个弹药 UI（关键发现，用户困惑点）**：
| UI | 面板 | 弹种来源 |
|---|---|---|
| 主页 GROUND VEHICLE LAB | groundWorkspacePanel | **弹药改造模块**（BeltOptions=modifications：STOCK + 3BM42_APDS_FS/ATGM/HE 包）——没有具体 3BM60 |
| EXPERIMENTAL → CONFIGURE GROUND VEHICLE | **同一个 groundWorkspacePanel**（复用） | 同上 |
| GROUND CONFIGURE 按钮 → 独立窗口 | ModernGroundConfigureWindow | **catalog**（ground_ammo.tsv：125 mm 3Bm60 等具体弹种） |

**结论**：想选具体弹种（3BM60）用 **GROUND CONFIGURE 窗口**（主页只有改造包）；选完要点 **MOUNT ROUND** 按钮（点列表项只是选中，不会自动装填）。

**待办（下次继续）**：
1. 编译弹药 fallback 修复 + 验证 3BM60 装填链路（窗口选 3BM60 → MOUNT → 数量 → APPLY → 生成 → 游戏内验证）
2. GroundDefaultRoundCount 是死代码（定义了没被调用，belt 链数逻辑闲置）
3. 窗口 currentCannon 可能未初始化（构造没填 cannonSelector → 弹种列表显示全量 catalog）——与主页列表不一致的根源
4. 主页改造包 vs 窗口具体弹种的写入差异（modification:t= 改造名 vs bulletsN:t=bulletName）

