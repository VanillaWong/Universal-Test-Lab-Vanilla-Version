# UTL 会话归档（2026-08-31）

> 完整状态快照。新会话务必先读本文件（+ ARCHIVE_2026-08-28.md 的历史）。

## 项目状态

- 路径：`C:\Users\honam\Desktop\Universal-Test-Lab-0.12.0-beta.2_modified\Universal-Test-Lab-0.12.0-beta.2`
- 编译：**`compile.bat`（双击即可）** 或 `csc /lib:C:\Windows\Microsoft.NET\Framework64\v4.0.30319\WPF @build.rsp`
- 最新编译：08:0x（机场功能最终版，**已验证可用**）
- beta.3 能玩版（GitHub release）：`C:\Users\honam\Desktop\Universal-Test-Lab-Vanilla-Version-0.12.0-beta.3\Universal-Test-Lab-Vanilla-Version-0.12.0-beta.3`
- GitHub：`VanillaWong/Universal-Test-Lab-Vanilla-Version`（main；beta.3 是能玩的 release）
- 用户成本敏感：**省 vision / 省 token**——少截图、少废话、能自己编译的自己编译（打包/推送才找 AI）

## 一、机场功能：全部尝试与结论（不要再走弯路）

1. **dynaf 变体**：`pg_1line_ground`（土路——显示过）、`2000_universal` / `3000_green`（在 Clean Test Range **不画视觉**——dynaf 跑道视觉依赖地图机场数据）；dynaf 拱起 95m（y 无关，固定抬升）
2. **rendInst 跑道**（`utl_airfield_runway.blk`，`gameData/objectGroups/`，composit+node+dummy_airfield_1200x250m_a+placeOnCollision）：显示过（"两条跑道"时期）；**y 必须 = 地形高度（外观跑道 = 32m）**，否则埋地下看不见
3. **addAirfield + spawnOnAirfield**（官方机制，create_spawns 触发器 initMission）：滑行起飞 + visibleOnHud 雷达标记（用户要的"蓝色棒子"）——**这是正确的机场功能方式**
4. **airfields_area**（大球区域）：识别但不画跑道——无效
5. **missionMarkAsRespawnPoint**（isAirfield=yes+target）：**不控制重生位置**；isAirfield 必须指向真机场单位（dynaf）
6. **重生机制（血泪结论）**：
   - `restoreType=manual + unitRespawn 触发器`：重生在 target（跑道）但**贴地无起落架**（空中出生状态）
   - `restoreType=attempts`：死后**坠机点附近空中重生**（能重生但有限次数，死多退任务）
   - `missionMarkAsRespawnPoint`：**不影响**玩家重生位置
   - 死后被拉到跑道贴地的元凶：**addAirfield 机场**（死后自动机场重生）——空中出生模式要移除机场才不拉走
   - 玩家出生/重生区域是**代码注入**（`ConfigureInstantPlayerRespawn` 的 `UTL_Player_Air_Spawn`，y=1500 空中或 y=32 跑道）——**不在模板**
7. **最终方案（用户选定）**：全空中出生/重生（restoreType=attempts）+ addAirfield 机场（可降落+标记）——出生不滑行（无 spawnOnAirfield），机场只做可降落

## 一b、机场功能最终状态（2026-08-31 晚，**已实现并验证**）

- 方案：`addAirfield`（教学格式 create_spawns 触发器，initMission）+ 4 个机场 zones + `visibleOnHud`（蓝色棒子）+ **引擎降落补给** + attempts 空中出生
- 当前设置：补给区 **1200m**（z:-24.9~1175.1，宽 runwayWidth=10）+ **start/end 已对调**（start z=-24.9 / end z=1175.1）+ 全部 **y=30**（=该处地形高度）+ spawn01 中段 (551.7,30,575.1)
- **死后拉人已解决**：代码 `respawnTarget = spawn`（=UTL_Player_Air_Spawn 空中出生点）——不再指向机场 spawn01
- **不再写主 blk**（只写 hot——beta.3 方式，游戏扫描主 blk 会严格解析）
- **Airfield_Runway（utl_airfield_runway 跑道物体）弃用**：placeOnCollision 使 y 失效 → 90m 空气墙/目标被顶起/崩溃
- **引擎补给条件**：降落在 runwayStart→end 之间的跑道面（宽=runwayWidth=10，**窄**——'补给刁钻'的原因）
- F 键按键补给（UTL Quick Restore：弹药+部件+燃油）保留
- 验证结果：不崩、列表显示、蓝色棒子、降落补给生效、死后不再拉人 ✓

## 二、blk 格式坑（反复踩，务必遵守）

- **行内多字段（bool/int）→ FATAL ERROR**（`invalid xxx (type) value`）：`is_enabled:b=yes comments:t=""` 同行、`runwayWidth:r =10 army:i =1 spawnPoint:t = "spawn01"` 三字段同行、`object_marking:i=0 object_var_name:t=""`——全部崩过。**任何块内字段必须每行一个**
- **必须逐行 + 带空格**（pve/官方格式）：`army:i = 1`、`isAirfield:b = yes`（等号两边空格）
- **zones 单行格式（`objLayer:i=4 props{}` 行内空 props）会导致任务列表隐藏**（旧结论'是 OK 的'**错误**）——必须多行：`objLayer:i=0` 独立行 + `props{}` 独立行 + tm 带空格 `[[150,0,0] [0,100,0] [0,0,250] [x,y,z]]`
- **create_spawns 块头必须独立行**：`} create_spawns{` 同行 → create_spawns 被吞、addAirfield 失效（但也不报错）
- **模板行尾统一 LF**（CRLF 主体 + LF 机场块混合 → 隐藏隐患）
- **armada 块缩进必须 2 空格**（代码 `UnitBlockByName` 用 `"  armada{"` 定位单位块——缩进错 → UTL 报 "Mission unit block is damaged: xxx"）
- **y = 游戏内海拔**——zones/物体 y = 地形高度（实测机场处 30m）

## 三、任务列表不显示问题（**已解决 2026-08-31 晚**）

- **根因**：机场内容的 blk 格式问题（zones 单行 objLayer:i=4 props{} / create_spawns 块头同行 / 行内多字段 / 混合行尾）→ 游戏解析失败 → 静默隐藏整个文件夹
- **修复**：教学格式（zones 多行 + 块头独立 + 全 LF + 只写 hot + 无 Airfield_Runway）→ 列表显示 ✓
- **addAirfield 本身是安全的**（官方 sample 里也有 airfield 字样）——之前隐藏全是格式问题

## 三b、任务列表不显示问题（历史过程——未解决时）

- 现象：UserMissions 列表看不到 Universal Test Lab（Ask3lad 等其他文件夹显示）
- 已尝试（全部无效）：usr.csv 文件名（usr.csv vs usr_前缀）、文件夹空格（UniversalTestLab vs Universal Test Lab）、注册行（missions/xxx;）、CRLF/表头、Ask3lad 风格 usr.csv、主 blk
- **已确认根因之一**：`CleanupPreviousGeneratedFiles`（L6436）清理 `universal_test_lab*.blk` 会**误删主任务文件**（不带 hot）→ 已修复（只删带下划线的 hot/start + 写 hot 时同时写主 blk）
- **beta.3（能玩）对比结论**：
  - **ModernShell.cs：两个版本完全相同**（字节数一样）——GARAGE/TARGETS/OPTIONS/EXPERIMENTAL、config.json 等 UI 功能 beta.3 全有
  - **UniversalTestLab.cs：只差约 2.5KB**——我们多了 addAirfield + spawnOnAirfield + 机场 zones + attempts（beta.3 用 manual+unitRespawn）
  - usr/文件夹/template：**已恢复 beta.3 原样**（`Universal Test Lab` 带空格 + `usr.csv` 不带前缀 + template 含 name/desc/date/location，LF 行尾）
- **13:39:21 编译（恢复 beta.3 配置）待用户验证**（重新生成 + 重启游戏看列表）
- 下一步（若仍不显示）：**UniversalTestLab.cs 逐块 diff beta.3**（找机场改动之外的差异）
- 游戏目录：`...\War Thunder\UserMissions\Universal Test Lab`（beta.3 的能玩文件夹——仍在）

## 三c、本次会话踩坑追加（重要）

- **模板改动必须重新编译才生效**（exe 嵌入模板）——而且**旧 exe 跑一次 UTL 会把嵌入式模板写回项目 template 目录**（覆盖手工改动！）——改模板 → 必须 compile.bat
- 验证流程：编译（确认 exe 时间戳 > 模板）→ 跑 UTL → 重启游戏——三步缺一不可
- 游戏文件夹残留的旧主 blk（universal_test_lab.blk）会被游戏扫描导致崩溃——删掉/改名
- 生成文件逐字节检查是根因定位的关键手段（多次用上）

## 三d、机场功能最终状态（已完成并验证，2026-08-31 晚）

**两个版本（同一 exe + OPTIONS 的 Spawn Mode 切换）**：
- **Air spawn (with speed)**（默认）：空中出生（UTL_Player_Air_Spawn 高空）+ restoreType=attempts + 死后 unitRespawn 空中重生
- **Airport takeoff (stationary)**：机场滑行起飞——initMission create_spawns 里 addAirfield + **spawnOnAirfield**（objects:t="UTL_AIRPORT_OBJECTS"→代码替换为 You）→ 跑道起点地面滑跑起飞；**死后重生：restoreType=manual（防 attempts 引擎抢重生/卡跳伞）+ 触发器 wait(2.1s) → spawnOnAirfield → 回跑道地面重生再滑行**

**关键结论（血泪）**：
- **unitRespawn 贴地重生是引擎限制**（重生到地面点会失败→永远卡跳伞界面，表现为"设定 2 秒但 20 秒不重生"）；**死后回跑道地面必须用 spawnOnAirfield 动作**（"Spawn selected units on the airfield on the ground"——官方 Triggers 文档 + Factory Defense 教学 + sample02_berlin 官方写法）
- spawnOnAirfield 官方格式：`runwayName:t="<airfield_start>"` + `objects:t="<单位>"`；可加多个 spawnPoint（官方用 01~03）
- addAirfield 教学格式：runwayStart/runwayEnd/runwayWidth=10/army=1/spawnPoint/visibleOnHud（等号空格与否都能解析——之前"必须等号空格"是行内多字段问题的误记）
- 引擎补给 = 降落在 runwayStart→runwayEnd 跑道面（宽=runwayWidth=10，窄！）；补给区由 zones 决定（airfield_area Box）
- 死后拉人 = addAirfield 固有（死后自动机场重生）；代码 respawnTarget=spawn（空中出生点）解决空中模式拉人

**当前机场设置**：补给区 **1250m**（z:-74.9 ~ 1175.1，宽100m，y=30，向 vidar 方向偏移 20m 到 x=551.7）；airfield_start z=-74.9（南）、airfield_end z=1175.1（北）、spawn01 z=575.1（中段）；start/end 已对调
- Airfield_Runway（utl_airfield_runway 跑道物体）**弃用**（placeOnCollision 使 y 失效→90m 空气墙/崩溃/目标被顶起）
- 模板不写主 blk（只写 hot）；模板全 LF；create_spawns/addAirfield/spawnOnAirfield 全部每行一个字段（行内多字段必炸 FATAL ERROR）

## 三e、对话压缩前最终状态（2026-08-31 深夜——新会话必读）

**机场功能全部完成并验证（两个版本，同一 exe + OPTIONS 的 Spawn Mode 切换）**：
- **Air spawn (with speed)**（默认）：空中出生 + restoreType=attempts + 死后 unitRespawn 空中重生（UTL_Player_Air_Spawn）
- **Airport takeoff (stationary)**：机场滑行起飞——initMission create_spawns 里 addAirfield + spawnOnAirfield（objects:t="UTL_AIRPORT_OBJECTS"→代码替换为 You）→跑道起点地面滑跑起飞；死后重生：**restoreType=manual + 触发器 wait(2.1s) → spawnOnAirfield → 回跑道地面重生再滑行**（已验证成功）

**机场关键数值（模板当前值）**：
- 补给区 1250m：airfield_start z=-74.9（南）、airfield_end z=1175.1（北）、spawn01 z=575.1（中段）、x=551.7（向 vidar 偏移 20m）、y=30（地形高度）
- runwayWidth=10（引擎补给面窄条 1250×10m——"补给刁钻"原因）；airfield_area Box z 半尺寸 625
- visibleOnHud=yes（蓝色棒子雷达标记）；restoreType 由代码按模式注入（manual/attempts）

**本次全部踩坑（血泪，勿重蹈）**：
1. **行内多字段必炸 FATAL ERROR**（`is_enabled:b=yes comments:t=""`同行、`runwayWidth/army/spawnPoint`同行）——create_spawns/addAirfield/spawnOnAirfield 内部**必须每行一个字段**（等号空格与否都能解析）
2. **Airfield_Runway（utl_airfield_runway 跑道物体）弃用**——placeOnCollision 使 y 失效（90m 空气墙/崩溃/目标被顶起）；无视觉跑道（地图无跑道视觉，靠蓝色棒子导航）
3. **模板改动必须 compile.bat 重编译**——exe 嵌入模板；**旧 exe 跑一次 UTL 会把嵌入式模板写回项目 template 目录覆盖手工改动**（曾导致改动丢失）
4. 游戏残留旧主 blk（universal_test_lab.blk 非 hot）会被扫描崩溃——已不再写主 blk（只写 hot）
5. **armada 块缩进必须 2 空格**——UTL 代码 UnitBlockByName 用 `"  armada{"`（两空格）定位单位块，缩进错 → "Mission unit block is damaged"
6. **unitRespawn 贴地重生是引擎限制**（重生到地面点失败→永远卡跳伞，表现为设定 2 秒但 20 秒不重生）；**死后回跑道地面必须用 spawnOnAirfield 动作**（官方 Triggers 文档："Spawn selected units on the airfield on the ground"）
7. spawnOnAirfield 的 objects 必须**无空格** `objects:t="UTL_AIRPORT_OBJECTS"`（代码 Replace 按无空格匹配；等号空格会替换失败→玩家 1500m 0 速空中出生）
8. 代码里**两个方法都有 `string trigger = @"`**——插入变量定义时注意别匹配错位置（曾致 CS0103 编译错误：respawnActions/respawnDelay/respawnTarget 不在上下文）
9. CDK 无触发器文档（vromfs 打包）；**官方 sample 任务是活文档**：UserMissions 根目录 sample01_bulge.blk / sample02_berlin.blk（addAirfield 多 spawnPoint、spawnOnAirfield{runwayName,objects}、missionMarkAsWaypoint 写法）

**已发布**：GitHub release **v0.12.0-beta.4**（注意 v0.12.0-beta.3 被 vanilla 原版占用）——https://github.com/VanillaWong/Universal-Test-Lab-Vanilla-Version/releases/tag/v0.12.0-beta.4；zip 在 dist\Universal_Test_Lab_v0.12.0-beta.4.zip（26.5MB）；main 已推送（121efea/8de55ed）；发布流程=Package-Release.ps1 -Version → git commit/tag → git credential 拿 token 调 API 建 release + uploads.github.com 传 asset

**模板/代码当前状态**：模板全 LF、create_spawns（教学格式全拆行）+addAirfield+spawnOnAirfield、F 键按键补给（UTL Quick Restore）保留；代码 respawnTarget=spawn、不写主 blk、airportTakeoff 分支完整（restoreTypeValue=manual/attempts、respawnActions=spawnOnAirfield/unitRespawn）；备份在 backup\（20260831_final）

## 四、未验证 / 待办

1. ~~列表不显示~~（已解决）
2. 跑道视觉最终确认（Airfield_Runway 弃用后无视觉跑道——暂不折腾）
3. 老待办：部分修部件名实测（drive_wheel/idler/autoloader）、BUK 目录验证

## 五、其他既定结论（历史沉淀）

- 统一 config.json（`%LOCALAPPDATA%\UniversalTestLab\config.json`）管理全部用户配置；`--config-dir` / `--selftest-config` / `--selftest-session` 测试开关
- 弹药槽按武器分组（复刻原版试驾 UI）；belt_type_limit 接入；多联装累加规则；GroundCalibre 正则 `(\d+)(?:_\d+)?mm`
- vehicle_weapons.json 预构建（Build-Catalog `-PhaseOnly 5`，51 秒）；GroundWeaponCache 内存缓存
- 5 个标签全部填充（VEHICLE/TARGETS/OPTIONS/GARAGE/EXPERIMENTAL）
- 分享包：`dist\Universal_Test_Lab_v0.12.0-beta.2.zip`（25MB，含中文教学 CHINESE_TUTORIAL.md）；Package-Release.ps1 打包
